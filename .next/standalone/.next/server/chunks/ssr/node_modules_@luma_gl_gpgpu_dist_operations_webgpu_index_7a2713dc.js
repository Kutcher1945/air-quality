module.exports=[18124,a=>{"use strict";var b=a.i(53099),c=a.i(14373),d=a.i(41821),e=a.i(19246),f=a.i(91133);let g=`fn arithmetic_add(x: {TYPE}, y: {TYPE}) -> {TYPE} {
  return x + y;
}

fn arithmetic_subtract(x: {TYPE}, y: {TYPE}) -> {TYPE} {
  return x - y;
}

fn arithmetic_multiply(x: {TYPE}, y: {TYPE}) -> {TYPE} {
  return x * y;
}

fn arithmetic_divide(x: {TYPE}, y: {TYPE}) -> {TYPE} {
  return x / y;
}

fn arithmetic_tan(x: f32) -> f32 {
  return tan_fp32(x);
}
`,h=`\
fn row_dot(x: array<{TYPE}, {X_LEN}>, y: array<{TYPE}, {Y_LEN}>) -> array<f32, 1> {
  var sum = 0.0;
  for (var i = 0u; i < {X_LEN}u; i = i + 1u) {
    sum += f32(x[i]) * f32(y[i]);
  }
  return array<f32, 1>(sum);
}
`,i=`\
fn equalAll(x: array<{TYPE}, {X_LEN}>, y: array<{TYPE}, {Y_LEN}>) -> array<u32, 1> {
  var allEqual = 1u;
  for (var i = 0u; i < {X_LEN}u; i = i + 1u) {
    if (x[i] != y[i]) {
      allEqual = 0u;
      break;
    }
  }
  return array<u32, 1>(allEqual);
}
`;var j=a.i(56665),k=a.i(71827),l=a.i(58764),m=a.i(34310);function n(a,b,c){let d=(0,f.getWGSLType)(b.type);return`@group(0) @binding(${c}) var<storage, read> ${a}: array<${d}>;`}function o(a,b,c,d=a){let e=(0,f.getWGSLType)(c);if(b.isConstant){let a=b.value;if(!a)throw Error(`Constant input ${b} is missing CPU values`);return`fn read_${d}(_sourceIndex: u32) -> array<${e}, ${b.size}> {
  return array<${e}, ${b.size}>(${Array.from({length:b.size},(b,c)=>(0,f.getLiteralValue)(e,a[c]??0)).join(", ")});
}`}let g=b.stride/b.ValueType.BYTES_PER_ELEMENT,h=b.offset/b.ValueType.BYTES_PER_ELEMENT,i=(0,f.getWGSLType)(b.type)===e?"":`${e}`;return`fn read_${d}(sourceIndex: u32) -> array<${e}, ${b.size}> {
  var value: array<${e}, ${b.size}>;
  let rowOffset = ${h}u + sourceIndex * ${g}u;
${Array.from({length:b.size},(b,c)=>i?`  value[${c}] = ${i}(${a}[rowOffset + ${c}u]);`:`  value[${c}] = ${a}[rowOffset + ${c}u];`).join("\n")}
  return value;
}`}function p(a,b){return o("sourceValues",a,b,"source_values")}function q(a,b){let c=(0,f.getWGSLType)(a.type);return`@group(0) @binding(${b}) var<storage, read_write> result: array<${c}>;`}function r(a){let b=a.stride/a.ValueType.BYTES_PER_ELEMENT,c=a.offset/a.ValueType.BYTES_PER_ELEMENT,d=(0,f.getWGSLType)(a.type);return`fn write_result(rowIndex: u32, value: array<${d}, ${a.size}>) {
  let rowOffset = ${c}u + rowIndex * ${b}u;
${Array.from({length:a.size},(a,b)=>`  result[rowOffset + ${b}u] = value[${b}];`).join("\n")}
}`}let s=`\
const LE: bool = ${new Uint8Array(new Uint16Array([255]).buffer)[0]>0?"true":"false"};
const F32_NAN: u32 = 0xffffffffu;
const F32_INF: u32 = 0x7f800000u;

fn roundShiftRight(value: u32, shift: i32) -> u32 {
  if (shift <= 0) {
    return value << u32(-shift);
  }

  if (shift >= 32) {
    if (shift == 32 && value > 0x80000000u) {
      return 1u;
    }
    return 0u;
  }

  let shiftU32 = u32(shift);
  let truncated = value >> shiftU32;
  let halfShift = 1u << u32(shift - 1);
  let remainder = value & ((1u << shiftU32) - 1u);
  if (remainder > halfShift || (remainder == halfShift && (truncated & 1u) == 1u)) {
    return truncated + 1u;
  }
  return truncated;
}

fn makeFloatImmediate(sign: u32, exponent: i32, mantissa: u32) -> u32 {
  return (sign << 31u) | (u32(exponent + 127) << 23u) | (mantissa & 0x7fffffu);
}

fn makeFloat(sign: u32, exponent: i32, significand: u32) -> u32 {
  if (significand == 0u) {
    return sign << 31u;
  }

  let leadingZeros = i32(countLeadingZeros(significand));
  var normalizedExponent = exponent + 31 - leadingZeros;

  if (normalizedExponent > 127) {
    return (sign << 31u) | F32_INF;
  }

  var mantissa: u32;
  if (normalizedExponent >= -126) {
    mantissa = roundShiftRight(significand, 8 - leadingZeros);
    if (mantissa >= 0x1000000u) {
      mantissa = mantissa >> 1u;
      normalizedExponent += 1;
      if (normalizedExponent > 127) {
        return (sign << 31u) | F32_INF;
      }
    }
    return makeFloatImmediate(sign, normalizedExponent, mantissa);
  }

  let subnormalShift = -149 - exponent;
  mantissa = roundShiftRight(significand, subnormalShift);
  if (mantissa >= 0x800000u) {
    return (sign << 31u) | (1u << 23u);
  }
  return (sign << 31u) | mantissa;
}

fn parseAsDouble(words: vec2<u32>) -> vec2<u32> {
  var d = words;
  if (LE) {
    d = d.yx;
  }

  let sign = (d.x >> 31u) & 1u;
  let exponentBits = (d.x >> 20u) & 0x7ffu;
  let exponent = i32(exponentBits) - 1023;
  let fractionHigh = d.x & 0xfffffu;
  let fractionLow = d.y;

  if (exponentBits == 0x7ffu) {
    if (fractionHigh == 0u && fractionLow == 0u) {
      return vec2<u32>((sign << 31u) | F32_INF, F32_NAN);
    }
    return vec2<u32>(F32_NAN);
  }

  if (exponentBits == 0u) {
    return vec2<u32>(sign << 31u);
  }

  if (exponent > 127) {
    return vec2<u32>((sign << 31u) | F32_INF, ((1u - sign) << 31u) | F32_INF);
  }

  let highSignificand = 0x800000u | (fractionHigh << 3u) | (fractionLow >> 29u);
  let lowSignificand = fractionLow & 0x1fffffffu;

  if (exponent < -126) {
    let highPart = makeFloat(sign, exponent - 23, highSignificand);
    let lowPart = makeFloat(sign, exponent - 52, lowSignificand);
    return vec2<u32>(highPart, lowPart);
  }

  let roundUp = lowSignificand > 0x10000000u ||
    (lowSignificand == 0x10000000u && (highSignificand & 1u) == 1u);

  var roundedSignificand = highSignificand + select(0u, 1u, roundUp);
  var highExponent = exponent;
  if (roundedSignificand == 0x1000000u) {
    roundedSignificand = 0x800000u;
    highExponent += 1;
  }

  if (highExponent > 127) {
    return vec2<u32>((sign << 31u) | F32_INF, ((1u - sign) << 31u) | F32_INF);
  }

  let highPart = makeFloatImmediate(sign, highExponent, roundedSignificand);

  var remainder = i32(lowSignificand);
  var lowSign = sign;
  if (roundUp) {
    remainder -= 0x20000000;
  }
  if (remainder < 0) {
    lowSign = 1u - sign;
    remainder = -remainder;
  }

  let lowPart = makeFloat(lowSign, exponent - 52, u32(remainder));
  return vec2<u32>(highPart, lowPart);
}

fn fround(x: array<u32, {X_LEN}>) -> array<f32, {RESULT_LEN}> {
  var result: array<f32, {RESULT_LEN}>;
  let n = {X_LEN}u / 2u;
  for (var i = 0u; i < n; i = i + 1u) {
    let parts = parseAsDouble(vec2<u32>(x[i * 2u], x[i * 2u + 1u]));
    result[i] = bitcast<f32>(parts.x);
    result[i + n] = bitcast<f32>(parts.y);
  }
  return result;
}
`,t=async({inputs:a,output:b,target:c})=>{var d,e,g,h,i,k;let l,o,s,{ids:t,sourceValues:u}=a,v=(0,f.getWGSLType)(t.type),w=[];t.isConstant||w.push({name:"ids",input:t,index:w.length}),u.isConstant||w.push({name:"sourceValues",input:u,index:w.length});let x=(0,m.getWebGPUDispatchLayout)(Math.ceil(b.length/64),c.device.limits.maxComputeWorkgroupsPerDimension),y=`
${w.map(({name:a,input:b,index:c})=>n(a,b,c)).join("\n")}
${function(a,b){if(a.isConstant){let c=a.value;if(!c)throw Error(`Constant input ${a} is missing CPU values`);return`fn read_ids(_rowIndex: u32) -> ${b} {
  return ${(0,f.getLiteralValue)(b,c[0]??0)};
}`}let c=a.stride/a.ValueType.BYTES_PER_ELEMENT,d=a.offset/a.ValueType.BYTES_PER_ELEMENT;return`fn read_ids(rowIndex: u32) -> ${b} {
  let rowOffset = ${d}u + rowIndex * ${c}u;
  return ids[rowOffset];
}`}(t,v)}
${p(u,b.type)}
${q(b,w.length)}
${r(b)}
${(d=b.type,e=b.size,s=(0,f.getZeroValue)(d),`fn zero_result() -> array<${(0,f.getWGSLType)(d)}, ${e}> {
  var result: array<${(0,f.getWGSLType)(d)}, ${e}>;
${Array.from({length:e},(a,b)=>`  result[${b}] = ${s};`).join("\n")}
  return result;
}`)}
${(g=t.type,h=b.type,i=b.size,k=u.length,l=(0,f.getWGSLType)(g),o=(0,f.getWGSLType)(h),`fn gather(idsValue: ${l}) -> array<${o}, ${i}> {
  let sourceIndex = ${"u32"===l?"i32(idsValue)":"i32"===l?"idsValue":"i32(idsValue)"};
  if (sourceIndex < 0 || sourceIndex >= ${k}) {
    return zero_result();
  }
  return read_source_values(u32(sourceIndex));
}`)}

@compute @workgroup_size(64) fn main(
  @builtin(workgroup_id) workgroupId: vec3<u32>,
  @builtin(local_invocation_id) localId: vec3<u32>
) {
  let rowIndex = ${(0,m.getWebGPUDispatchRowIndex)(x,64)};
  if (rowIndex >= ${b.length}u) {
    return;
  }

  let idsValue = read_ids(rowIndex);
  let result = gather(idsValue);
  write_result(rowIndex, result);
}
`,z=new j.Computation(c.device,{source:y,shaderLayout:{bindings:[...w.map(({name:a,index:b})=>({name:a,type:"storage",group:0,location:b})),{name:"result",type:"storage",group:0,location:w.length}]}}),A={};t.isConstant||(A.ids=t.buffer),u.isConstant||(A.sourceValues=u.buffer),A.result=c,z.setBindings(A);let B=c.device.beginComputePass({});return z.dispatch(B,x.x,x.y,x.z),B.end(),c.device.submit(),z.destroy(),{success:!0}},u=async({inputs:a,output:b,target:c})=>{var d;let{segments:e}=a,f=e.isConstant?[]:[{name:"segments",input:e,index:0}],g=(0,m.getWebGPUDispatchLayout)(Math.ceil(b.length/64),c.device.limits.maxComputeWorkgroupsPerDimension),h=`
${f.map(({name:a,input:b,index:c})=>n(a,b,c)).join("\n")}
${o("segments",e,"uint32")}
${q(b,f.length)}
${r(b)}
${(d=e.length,`fn segmented_map(vertexIndex: u32) -> array<u32, 2> {
  var low = 0i;
  var high = ${d}i;
  while (low < high) {
    let mid = low + (high - low) / 2i;
    let midStart = read_segments(u32(mid))[0];
    if (midStart <= vertexIndex) {
      low = mid + 1i;
    } else {
      high = mid;
    }
  }

  let segmentIndex = u32(max(low - 1i, 0i));
  let segmentStart = read_segments(segmentIndex)[0];
  return array<u32, 2>(segmentIndex, vertexIndex - segmentStart);
}`)}

@compute @workgroup_size(64) fn main(
  @builtin(workgroup_id) workgroupId: vec3<u32>,
  @builtin(local_invocation_id) localId: vec3<u32>
) {
  let rowIndex = ${(0,m.getWebGPUDispatchRowIndex)(g,64)};
  if (rowIndex >= ${b.length}u) {
    return;
  }

  let result = segmented_map(rowIndex);
  write_result(rowIndex, result);
}
`,i=new j.Computation(c.device,{source:h,shaderLayout:{bindings:[...f.map(({name:a,index:b})=>({name:a,type:"storage",group:0,location:b})),{name:"result",type:"storage",group:0,location:f.length}]}}),k=Object.fromEntries(f.map(({name:a,input:b})=>[a,b.buffer]));k.result=c,i.setBindings(k);let l=c.device.beginComputePass({});return i.dispatch(l,g.x,g.y,g.z),l.end(),c.device.submit(),i.destroy(),{success:!0}};var v=a.i(12067);let w=`\
fn row_length(x: array<{TYPE}, {X_LEN}>) -> array<f32, 1> {
  var sum = 0.0;
  for (var i = 0u; i < {X_LEN}u; i = i + 1u) {
    sum += f32(x[i]) * f32(x[i]);
  }
  return array<f32, 1>(sqrt(sum));
}
`,x=async({inputs:a,output:b,target:c})=>{let d=(0,f.getZeroValue)(b.type);return(0,e.runRowComputation)({module:{name:"select",source:"// inline expression select\n"},inputs:a,output:b,operationType:b.type,outputBuffer:c,expression:b=>{let c=y("condition",a.condition,b,d),e=y("whenTrue",a.whenTrue,b,d),f=y("whenFalse",a.whenFalse,b,d);return`select(${f}, ${e}, ${c} != ${d})`}}),{success:!0}};function y(a,b,c,d){return c<b.size?`${a}[${c}]`:1===b.size?`${a}[0]`:d}a.s([],6042),a.i(6042),a.s(["arithmetic",0,({inputs:a,output:h,target:i})=>{let j=h.type,k=(0,f.getWGSLType)(j),l=(0,f.getZeroValue)(j),m=a.namedInputs;return(0,e.runRowComputation)({module:{name:"arithmetic",source:g,dependencies:[b.fp32]},inputs:m,output:h,operationType:j,outputBuffer:i,expression:b=>(0,c.compileExpression)(a.expression,{operations:d.ARITHMETIC_OPERATIONS,inputs:m,laneIndex:b,formatInput:a=>`${a}[${b}]`,formatOutOfBoundsInput:a=>1===m[a].size?`${a}[0]`:l,formatLiteral:a=>{let c=Array.isArray(a)?a[b]??0:a;return`${k}(${(0,f.formatLiteralValue)(j,c)})`},formatCall:(a,b)=>`${a}(${b.join(", ")})`})}),{success:!0}},"dot",0,({inputs:a,output:b,target:c})=>((0,e.runRowComputation)({module:{name:"row_dot",source:h},inputs:a,output:b,operationType:"float32",outputBuffer:c}),{success:!0}),"equalAll",0,({inputs:a,output:b,target:c})=>((0,e.runRowComputation)({module:{name:"equalAll",source:i},inputs:a,output:b,operationType:a.x.type,outputBuffer:c}),{success:!0}),"extent",0,({inputs:a,output:b,target:c})=>{let{sourceValues:d}=a;if(0===d.length){let a=new b.ValueType(b.length*b.size);return c.write(a),{success:!0,value:a}}if(d.isConstant){let a=d.value;if(!a)throw Error(`Constant input ${d} is missing CPU values`);let e=new b.ValueType(b.length*b.size);for(let c=0;c<b.length;c++){let b=a[c];e[2*c]=b,e[2*c+1]=b}return c.write(e),{success:!0,value:e}}let e=[],g=d,h="raw",i=d.length;try{for(;;){let a=Math.ceil(i/64),d=b.length*a,o=1===a?c:l.bufferPool.createOrReuse(c.device,d*b.stride);if(a>1&&e.push(o),function({input:a,inputMode:b,inputGroupCount:c,channelCount:d,outputType:e,outputBuffer:g,outputLength:h,outputStride:i,outputOffset:l}){let o=(0,f.getWGSLType)(e),s=(0,m.getWebGPUDispatchLayout)(h,g.device.limits.maxComputeWorkgroupsPerDimension),t=new k.GPUDataEvaluator({buffer:g,type:e,size:2,length:h,stride:i,offset:l}),u=`
${a.isConstant?"":n("sourceValues",a,0)}
${p(a,e)}
${q(t,+!a.isConstant)}
${r(t)}
${function(a,b,c,d){let e=(0,f.getWGSLType)(b),[g,h]=function(a){switch(a){case"uint32":return["0xffffffffu","0u"];case"sint32":return["2147483647","-2147483648"];case"float32":return["3.402823e38","-3.402823e38"];default:throw Error(`Unsupported WebGPU extent type for ${a}`)}}(b);return"raw"===a?`fn extent_pass(channelIndex: u32, inputGroupIndex: u32) -> array<${e}, 2> {
  var result: array<${e}, 2>;
  result[0] = ${g};
  result[1] = ${h};

  if (inputGroupIndex < ${d}u) {
    let value = read_source_values(inputGroupIndex);
    result[0] = value[channelIndex];
    result[1] = value[channelIndex];
  }

  return result;
}`:`fn extent_pass(channelIndex: u32, inputGroupIndex: u32) -> array<${e}, 2> {
  var result: array<${e}, 2>;
  result[0] = ${g};
  result[1] = ${h};

  if (inputGroupIndex < ${d}u) {
    let rowIndex = inputGroupIndex * ${c}u + channelIndex;
    let value = read_source_values(rowIndex);
    result[0] = value[0];
    result[1] = value[1];
  }

  return result;
}`}(b,e,d,c)}

var<workgroup> sharedMin: array<${o}, 64>;
var<workgroup> sharedMax: array<${o}, 64>;

@compute @workgroup_size(64) fn main(
  @builtin(workgroup_id) workgroupId: vec3<u32>,
  @builtin(local_invocation_id) localId: vec3<u32>
) {
  let outputRowIndex = ${(0,m.getWebGPUDispatchWorkgroupIndex)(s)};
  if (outputRowIndex >= ${h}u) {
    return;
  }

  let channelIndex = outputRowIndex % ${d}u;
  let outputGroupIndex = outputRowIndex / ${d}u;
  let inputGroupIndex = outputGroupIndex * 64u + localId.x;

  let result = extent_pass(channelIndex, inputGroupIndex);
  sharedMin[localId.x] = result[0];
  sharedMax[localId.x] = result[1];
  workgroupBarrier();

  var stride = ${Math.floor(32)}u;
  loop {
    if (stride == 0u) {
      break;
    }
    if (localId.x < stride) {
      let compareIndex = localId.x + stride;
      if (sharedMin[compareIndex] < sharedMin[localId.x]) {
        sharedMin[localId.x] = sharedMin[compareIndex];
      }
      if (sharedMax[compareIndex] > sharedMax[localId.x]) {
        sharedMax[localId.x] = sharedMax[compareIndex];
      }
    }
    workgroupBarrier();
    stride = stride / 2u;
  }

  if (localId.x == 0u) {
    write_result(outputRowIndex, array<${o}, 2>(sharedMin[0], sharedMax[0]));
  }
}
`,v=new j.Computation(g.device,{source:u,shaderLayout:{bindings:[...a.isConstant?[]:[{name:"sourceValues",type:"storage",group:0,location:0}],{name:"result",type:"storage",group:0,location:+!a.isConstant}]}}),w={result:g};a.isConstant||(w.sourceValues=a.buffer),v.setBindings(w);let x=g.device.beginComputePass({});v.dispatch(x,s.x,s.y,s.z),x.end(),g.device.submit(),v.destroy()}({input:g,inputMode:h,inputGroupCount:i,channelCount:b.length,outputType:b.type,outputBuffer:o,outputLength:d,outputStride:b.stride,outputOffset:b.offset}),1===a)break;g=new k.GPUDataEvaluator({buffer:o,type:b.type,size:2,length:d}),h="partial",i=a}return{success:!0}}finally{for(let a of e)l.bufferPool.recycle(a)}},"fround",0,({inputs:a,output:b,target:c})=>((0,e.runRowComputation)({module:{name:"fround",source:s},inputs:a,output:b,operationType:"uint32",outputBuffer:c}),{success:!0}),"gather",0,t,"interleave",()=>v.interleave,"length",0,({inputs:a,output:b,target:c})=>((0,e.runRowComputation)({module:{name:"row_length",source:w},inputs:a,output:b,operationType:"float32",outputBuffer:c}),{success:!0}),"segmentedMap",0,u,"select",0,x,"sequence",0,({inputs:a,output:b,target:c})=>{let d=(0,m.getWebGPUDispatchLayout)(Math.ceil(b.length/64),c.device.limits.maxComputeWorkgroupsPerDimension),e=`\
@group(0) @binding(0) var<storage, read_write> result: array<i32>;

@compute @workgroup_size(64) fn main(
  @builtin(workgroup_id) workgroupId: vec3<u32>,
  @builtin(local_invocation_id) localId: vec3<u32>
) {
  let rowIndex = ${(0,m.getWebGPUDispatchRowIndex)(d,64)};
  if (rowIndex >= ${b.length}u) {
    return;
  }

  let rowOffset = ${b.offset/b.ValueType.BYTES_PER_ELEMENT}u + rowIndex * ${b.stride/b.ValueType.BYTES_PER_ELEMENT}u;
  result[rowOffset] = ${a.start} + i32(rowIndex) * ${a.step};
}
`,f=new j.Computation(c.device,{source:e,shaderLayout:{bindings:[{name:"result",type:"storage",group:0,location:0}]}});f.setBindings({result:c});let g=c.device.beginComputePass({});return f.dispatch(g,d.x,d.y,d.z),g.end(),c.device.submit(),f.destroy(),{success:!0}},"swizzle",0,({inputs:a,output:b,target:c})=>{let{columns:d}=a;return(0,e.runRowComputation)({module:{name:"swizzle",source:"// swizzle expression handled inline"},expression:a=>`x[${d[a]}]`,inputs:{x:a.x},output:b,outputBuffer:c}),{success:!0}}],18124)}];

//# sourceMappingURL=node_modules_%40luma_gl_gpgpu_dist_operations_webgpu_index_7a2713dc.js.map