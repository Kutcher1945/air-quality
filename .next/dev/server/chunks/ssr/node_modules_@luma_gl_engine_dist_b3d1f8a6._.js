module.exports = [
"[project]/node_modules/@luma.gl/engine/dist/animation/timeline.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "Timeline",
    ()=>Timeline
]);
let channelHandles = 1;
let animationHandles = 1;
class Timeline {
    time = 0;
    channels = new Map();
    animations = new Map();
    playing = false;
    lastEngineTime = -1;
    constructor(){}
    addChannel(props) {
        const { delay = 0, duration = Number.POSITIVE_INFINITY, rate = 1, repeat = 1 } = props;
        const channelId = channelHandles++;
        const channel = {
            time: 0,
            delay,
            duration,
            rate,
            repeat
        };
        this._setChannelTime(channel, this.time);
        this.channels.set(channelId, channel);
        return channelId;
    }
    removeChannel(channelId) {
        this.channels.delete(channelId);
        for (const [animationHandle, animation] of this.animations){
            if (animation.channel === channelId) {
                this.detachAnimation(animationHandle);
            }
        }
    }
    isFinished(channelId) {
        const channel = this.channels.get(channelId);
        if (channel === undefined) {
            return false;
        }
        return this.time >= channel.delay + channel.duration * channel.repeat;
    }
    getTime(channelId) {
        if (channelId === undefined) {
            return this.time;
        }
        const channel = this.channels.get(channelId);
        if (channel === undefined) {
            return -1;
        }
        return channel.time;
    }
    setTime(time) {
        this.time = Math.max(0, time);
        const channels = this.channels.values();
        for (const channel of channels){
            this._setChannelTime(channel, this.time);
        }
        const animations = this.animations.values();
        for (const animationData of animations){
            const { animation, channel } = animationData;
            animation.setTime(this.getTime(channel));
        }
    }
    play() {
        this.playing = true;
    }
    pause() {
        this.playing = false;
        this.lastEngineTime = -1;
    }
    reset() {
        this.setTime(0);
    }
    attachAnimation(animation, channelHandle) {
        const animationHandle = animationHandles++;
        this.animations.set(animationHandle, {
            animation,
            channel: channelHandle
        });
        animation.setTime(this.getTime(channelHandle));
        return animationHandle;
    }
    detachAnimation(channelId) {
        this.animations.delete(channelId);
    }
    update(engineTime) {
        if (this.playing) {
            if (this.lastEngineTime === -1) {
                this.lastEngineTime = engineTime;
            }
            this.setTime(this.time + (engineTime - this.lastEngineTime));
            this.lastEngineTime = engineTime;
        }
    }
    _setChannelTime(channel, time) {
        const offsetTime = time - channel.delay;
        const totalDuration = channel.duration * channel.repeat;
        // Note(Tarek): Don't loop on final repeat.
        if (offsetTime >= totalDuration) {
            channel.time = channel.duration * channel.rate;
        } else {
            channel.time = Math.max(0, offsetTime) % channel.duration;
            channel.time *= channel.rate;
        }
    }
} //# sourceMappingURL=timeline.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/animation-loop/request-animation-frame.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
/* global window, setTimeout, clearTimeout */ /** Node.js polyfill for requestAnimationFrame */ // / <reference types="@types/node" />
__turbopack_context__.s([
    "cancelAnimationFramePolyfill",
    ()=>cancelAnimationFramePolyfill,
    "requestAnimationFramePolyfill",
    ()=>requestAnimationFramePolyfill
]);
function requestAnimationFramePolyfill(callback) {
    const browserRequestAnimationFrame = ("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : null;
    if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
    ;
    return setTimeout(()=>callback(typeof performance !== 'undefined' ? performance.now() : Date.now()), 1000 / 60);
}
function cancelAnimationFramePolyfill(timerId) {
    const browserCancelAnimationFrame = ("TURBOPACK compile-time falsy", 0) ? "TURBOPACK unreachable" : null;
    if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
    ;
    clearTimeout(timerId);
} //# sourceMappingURL=request-animation-frame.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/animation-loop/animation-loop.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "AnimationLoop",
    ()=>AnimationLoop
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$luma$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/luma.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$animation$2d$loop$2f$request$2d$animation$2d$frame$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/animation-loop/request-animation-frame.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$probe$2e$gl$2f$stats$2f$dist$2f$index$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@probe.gl/stats/dist/index.js [app-ssr] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$probe$2e$gl$2f$stats$2f$dist$2f$lib$2f$stats$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__$3c$export__default__as__Stats$3e$__ = __turbopack_context__.i("[project]/node_modules/@probe.gl/stats/dist/lib/stats.js [app-ssr] (ecmascript) <export default as Stats>");
;
;
;
let statIdCounter = 0;
const ANIMATION_LOOP_STATS = 'Animation Loop';
const defaultAnimationFrameProvider = {
    requestAnimationFrame: (callback)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$animation$2d$loop$2f$request$2d$animation$2d$frame$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["requestAnimationFramePolyfill"])(callback),
    cancelAnimationFrame: (animationFrameId)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$animation$2d$loop$2f$request$2d$animation$2d$frame$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["cancelAnimationFramePolyfill"])(animationFrameId)
};
class AnimationLoop {
    static defaultAnimationLoopProps = {
        device: null,
        onAddHTML: ()=>'',
        onInitialize: async ()=>null,
        onRender: ()=>{},
        onFinalize: ()=>{},
        onError: (error)=>{
            // biome-ignore lint/suspicious/noConsole: default fallback error reporting for app loops.
            console.error(error);
        },
        stats: undefined,
        // view parameters
        autoResizeViewport: false,
        animationFrameProvider: defaultAnimationFrameProvider
    };
    device = null;
    canvas = null;
    props;
    animationProps = null;
    timeline = null;
    stats;
    sharedStats;
    cpuTime;
    gpuTime;
    frameRate;
    display;
    _needsRedraw = 'initialized';
    _initialized = false;
    _running = false;
    _animationFrameId = null;
    _nextFramePromise = null;
    _resolveNextFrame = null;
    _cpuStartTime = 0;
    _error = null;
    _lastFrameTime = 0;
    /*
     * @param {HTMLCanvasElement} canvas - if provided, width and height will be passed to context
     */ constructor(props){
        this.props = {
            ...AnimationLoop.defaultAnimationLoopProps,
            ...props
        };
        props = this.props;
        if (!props.device) {
            throw new Error('No device provided');
        }
        // state
        this.stats = props.stats || new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$probe$2e$gl$2f$stats$2f$dist$2f$lib$2f$stats$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__$3c$export__default__as__Stats$3e$__["Stats"]({
            id: `animation-loop-${statIdCounter++}`
        });
        this.sharedStats = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$luma$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["luma"].stats.get(ANIMATION_LOOP_STATS);
        this.frameRate = this.stats.get('Frame Rate');
        this.frameRate.setSampleSize(1);
        this.cpuTime = this.stats.get('CPU Time');
        this.gpuTime = this.stats.get('GPU Time');
        this.setProps({
            autoResizeViewport: props.autoResizeViewport,
            animationFrameProvider: props.animationFrameProvider
        });
        // Bind methods
        this.start = this.start.bind(this);
        this.stop = this.stop.bind(this);
        this._onMousemove = this._onMousemove.bind(this);
        this._onMouseleave = this._onMouseleave.bind(this);
    }
    destroy() {
        this.stop();
        this._setDisplay(null);
        this.device?._disableDebugGPUTime();
    }
    /** @deprecated Use .destroy() */ delete() {
        this.destroy();
    }
    reportError(error) {
        this._error = error;
        this.props.onError(error);
        if (this.props.onError === AnimationLoop.defaultAnimationLoopProps.onError && ("TURBOPACK compile-time value", "undefined") !== 'undefined' && typeof ErrorEvent !== 'undefined') //TURBOPACK unreachable
        ;
    }
    /** Flags this animation loop as needing redraw */ setNeedsRedraw(reason) {
        this._needsRedraw = this._needsRedraw || reason;
        return this;
    }
    /** Query redraw status. Clears the flag. */ needsRedraw() {
        const reason = this._needsRedraw;
        this._needsRedraw = false;
        return reason;
    }
    setProps(props) {
        if ('autoResizeViewport' in props) {
            this.props.autoResizeViewport = props.autoResizeViewport || false;
        }
        if ('animationFrameProvider' in props) {
            const animationFrameProvider = props.animationFrameProvider || defaultAnimationFrameProvider;
            if (animationFrameProvider !== this.props.animationFrameProvider) {
                const animationFrameWasScheduled = this._animationFrameId !== null;
                if (animationFrameWasScheduled) {
                    this._cancelAnimationFrame();
                }
                this.props.animationFrameProvider = animationFrameProvider;
                if (animationFrameWasScheduled) {
                    this._requestAnimationFrame();
                }
            }
        }
        return this;
    }
    /** Starts a render loop if not already running */ async start() {
        if (this._running) {
            return this;
        }
        this._running = true;
        try {
            let appContext;
            if (!this._initialized) {
                this._initialized = true;
                // Create the WebGL context
                await this._initDevice();
                this._initialize();
                if (!this._running) {
                    return null;
                }
                // Note: onIntialize can return a promise (e.g. in case app needs to load resources)
                await this.props.onInitialize(this._getAnimationProps());
            }
            // check that we haven't been stopped
            if (!this._running) {
                return null;
            }
            // Start the loop
            if (appContext !== false) {
                // cancel any pending renders to ensure only one loop can ever run
                this._cancelAnimationFrame();
                this._requestAnimationFrame();
            }
            return this;
        } catch (err) {
            const error = err instanceof Error ? err : new Error('Unknown error');
            this.props.onError(error);
            // this._running = false; // TODO
            throw error;
        }
    }
    /** Stops a render loop if already running, finalizing */ stop() {
        // console.debug(`Stopping ${this.constructor.name}`);
        if (this._running) {
            const animationProps = this.animationProps;
            // call callback
            // If stop is called immediately, we can end up in a state where props haven't been initialized...
            this._cancelAnimationFrame();
            this._nextFramePromise = null;
            this._resolveNextFrame = null;
            this._running = false;
            this._lastFrameTime = 0;
            if (animationProps) {
                this.props.onFinalize(animationProps);
            }
        }
        return this;
    }
    /** Explicitly draw a frame */ redraw(time, animationFrame = null) {
        if (this.device?.isLost || this._error) {
            return this;
        }
        this._beginFrameTimers(time);
        this._setupFrame();
        if (this.animationProps) {
            this.animationProps.animationFrame = animationFrame;
        }
        this._updateAnimationProps();
        this._renderFrame(this._getAnimationProps());
        // clear needsRedraw flag
        this._clearNeedsRedraw();
        if (this._resolveNextFrame) {
            this._resolveNextFrame(this);
            this._nextFramePromise = null;
            this._resolveNextFrame = null;
        }
        this._endFrameTimers();
        return this;
    }
    /** Add a timeline, it will be automatically updated by the animation loop. */ attachTimeline(timeline) {
        this.timeline = timeline;
        return this.timeline;
    }
    /** Remove a timeline */ detachTimeline() {
        this.timeline = null;
    }
    /** Wait until a render completes */ waitForRender() {
        this.setNeedsRedraw('waitForRender');
        if (!this._nextFramePromise) {
            this._nextFramePromise = new Promise((resolve)=>{
                this._resolveNextFrame = resolve;
            });
        }
        return this._nextFramePromise;
    }
    /** TODO - should use device.deviceContext */ async toDataURL() {
        this.setNeedsRedraw('toDataURL');
        await this.waitForRender();
        if (this.canvas instanceof HTMLCanvasElement) {
            return this.canvas.toDataURL();
        }
        throw new Error('OffscreenCanvas');
    }
    // PRIVATE METHODS
    _initialize() {
        this._startEventHandling();
        // Initialize the callback data
        this._initializeAnimationProps();
        this._updateAnimationProps();
        // Default viewport setup, in case onInitialize wants to render
        this._resizeViewport();
        this.device?._enableDebugGPUTime();
    }
    _setDisplay(display) {
        if (this.display) {
            this.display.destroy();
            this.display.animationLoop = null;
        }
        // store animation loop on the display
        if (display) {
            display.animationLoop = this;
        }
        this.display = display;
    }
    _requestAnimationFrame() {
        if (!this._running) {
            return;
        }
        this._animationFrameId = this.props.animationFrameProvider.requestAnimationFrame(this._animationFrame.bind(this));
    }
    _cancelAnimationFrame() {
        if (this._animationFrameId === null) {
            return;
        }
        this.props.animationFrameProvider.cancelAnimationFrame(this._animationFrameId);
        this._animationFrameId = null;
    }
    _animationFrame(time, animationFrame) {
        if (!this._running) {
            return;
        }
        try {
            this.redraw(time, animationFrame ?? null);
            this._requestAnimationFrame();
        } catch (error) {
            const renderError = error instanceof Error ? error : new Error(String(error));
            this.reportError(renderError);
            this.stop();
        }
    }
    // Called on each frame, can be overridden to call onRender multiple times
    // to support e.g. stereoscopic rendering
    _renderFrame(animationProps) {
        // Allow e.g. VR display to render multiple frames.
        if (this.display) {
            this.display._renderFrame(animationProps);
            return;
        }
        // call callback
        const renderResult = this.props.onRender(this._getAnimationProps());
        // end callback
        // Submit commands (necessary on WebGPU).
        if (this.device && renderResult !== false) {
            this.device.submit();
        }
    }
    _clearNeedsRedraw() {
        this._needsRedraw = false;
    }
    _setupFrame() {
        this._resizeViewport();
    }
    // Initialize the  object that will be passed to app callbacks
    _initializeAnimationProps() {
        const canvasContext = this.device?.getDefaultCanvasContext();
        if (!this.device || !canvasContext) {
            throw new Error('loop');
        }
        const canvas = canvasContext?.canvas;
        const useDevicePixels = canvasContext.props.useDevicePixels;
        this.animationProps = {
            animationLoop: this,
            device: this.device,
            canvasContext,
            canvas,
            // @ts-expect-error Deprecated
            useDevicePixels,
            timeline: this.timeline,
            needsRedraw: false,
            // Placeholders
            width: 1,
            height: 1,
            aspect: 1,
            // Animation props
            time: 0,
            startTime: Date.now(),
            engineTime: 0,
            tick: 0,
            tock: 0,
            // Experimental
            animationFrame: null,
            _mousePosition: null // Event props
        };
    }
    _getAnimationProps() {
        if (!this.animationProps) {
            throw new Error('animationProps');
        }
        return this.animationProps;
    }
    // Update the context object that will be passed to app callbacks
    _updateAnimationProps() {
        if (!this.animationProps) {
            return;
        }
        // Can this be replaced with canvas context?
        const { width, height, aspect } = this._getSizeAndAspect();
        if (width !== this.animationProps.width || height !== this.animationProps.height) {
            this.setNeedsRedraw('drawing buffer resized');
        }
        if (aspect !== this.animationProps.aspect) {
            this.setNeedsRedraw('drawing buffer aspect changed');
        }
        this.animationProps.width = width;
        this.animationProps.height = height;
        this.animationProps.aspect = aspect;
        this.animationProps.needsRedraw = this._needsRedraw;
        // Update time properties
        this.animationProps.engineTime = Date.now() - this.animationProps.startTime;
        if (this.timeline) {
            this.timeline.update(this.animationProps.engineTime);
        }
        this.animationProps.tick = Math.floor(this.animationProps.time / 1000 * 60);
        this.animationProps.tock++;
        // For back compatibility
        this.animationProps.time = this.timeline ? this.timeline.getTime() : this.animationProps.engineTime;
    }
    /** Wait for supplied device */ async _initDevice() {
        this.device = await this.props.device;
        if (!this.device) {
            throw new Error('No device provided');
        }
        this.canvas = this.device.getDefaultCanvasContext().canvas || null;
    // this._createInfoDiv();
    }
    _createInfoDiv() {
        if (this.canvas && this.props.onAddHTML) {
            const wrapperDiv = document.createElement('div');
            document.body.appendChild(wrapperDiv);
            wrapperDiv.style.position = 'relative';
            const div = document.createElement('div');
            div.style.position = 'absolute';
            div.style.left = '10px';
            div.style.bottom = '10px';
            div.style.width = '300px';
            div.style.background = 'white';
            if (this.canvas instanceof HTMLCanvasElement) {
                wrapperDiv.appendChild(this.canvas);
            }
            wrapperDiv.appendChild(div);
            const html = this.props.onAddHTML(div);
            if (html) {
                div.innerHTML = html;
            }
        }
    }
    _getSizeAndAspect() {
        if (!this.device) {
            return {
                width: 1,
                height: 1,
                aspect: 1
            };
        }
        // Match projection setup to the actual render target dimensions, which may
        // differ from the CSS size when device-pixel scaling or backend clamping applies.
        const [width, height] = this.device.getDefaultCanvasContext().getDrawingBufferSize();
        const aspect = width > 0 && height > 0 ? width / height : 1;
        return {
            width,
            height,
            aspect
        };
    }
    /** @deprecated Default viewport setup */ _resizeViewport() {
        // TODO can we use canvas context to code this in a portable way?
        // @ts-expect-error Expose on canvasContext
        if (this.props.autoResizeViewport && this.device.gl) {
            // @ts-expect-error Expose canvasContext
            this.device.gl.viewport(0, 0, // @ts-expect-error Expose canvasContext
            this.device.gl.drawingBufferWidth, // @ts-expect-error Expose canvasContext
            this.device.gl.drawingBufferHeight);
        }
    }
    _beginFrameTimers(time) {
        const now = time ?? (typeof performance !== 'undefined' ? performance.now() : Date.now());
        if (this._lastFrameTime) {
            const frameTime = now - this._lastFrameTime;
            if (frameTime > 0) {
                this.frameRate.addTime(frameTime);
            }
        }
        this._lastFrameTime = now;
        if (this.device?._isDebugGPUTimeEnabled()) {
            this._consumeEncodedGpuTime();
        }
        this.cpuTime.timeStart();
    }
    _endFrameTimers() {
        if (this.device?._isDebugGPUTimeEnabled()) {
            this._consumeEncodedGpuTime();
        }
        this.cpuTime.timeEnd();
        this._updateSharedStats();
    }
    _consumeEncodedGpuTime() {
        if (!this.device) {
            return;
        }
        const gpuTimeMs = this.device.commandEncoder._gpuTimeMs;
        if (gpuTimeMs !== undefined) {
            this.gpuTime.addTime(gpuTimeMs);
            this.device.commandEncoder._gpuTimeMs = undefined;
        }
    }
    _updateSharedStats() {
        if (this.stats === this.sharedStats) {
            return;
        }
        for (const name of Object.keys(this.sharedStats.stats)){
            if (!this.stats.stats[name]) {
                delete this.sharedStats.stats[name];
            }
        }
        this.stats.forEach((sourceStat)=>{
            const targetStat = this.sharedStats.get(sourceStat.name, sourceStat.type);
            targetStat.sampleSize = sourceStat.sampleSize;
            targetStat.time = sourceStat.time;
            targetStat.count = sourceStat.count;
            targetStat.samples = sourceStat.samples;
            targetStat.lastTiming = sourceStat.lastTiming;
            targetStat.lastSampleTime = sourceStat.lastSampleTime;
            targetStat.lastSampleCount = sourceStat.lastSampleCount;
            targetStat._count = sourceStat._count;
            targetStat._time = sourceStat._time;
            targetStat._samples = sourceStat._samples;
            targetStat._startTime = sourceStat._startTime;
            targetStat._timerPending = sourceStat._timerPending;
        });
    }
    // Event handling
    _startEventHandling() {
        if (this.canvas) {
            this.canvas.addEventListener('mousemove', this._onMousemove.bind(this));
            this.canvas.addEventListener('mouseleave', this._onMouseleave.bind(this));
        }
    }
    _onMousemove(event) {
        if (event instanceof MouseEvent) {
            this._getAnimationProps()._mousePosition = [
                event.offsetX,
                event.offsetY
            ];
        }
    }
    _onMouseleave(event) {
        this._getAnimationProps()._mousePosition = null;
    }
} //# sourceMappingURL=animation-loop.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/utils/uid.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "uid",
    ()=>uid
]);
const uidCounters = {};
function uid(id = 'id') {
    uidCounters[id] = uidCounters[id] || 1;
    const count = uidCounters[id]++;
    return `${id}-${count}`;
} //# sourceMappingURL=uid.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/dynamic-buffer/dynamic-buffer.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "DynamicBuffer",
    ()=>DynamicBuffer,
    "getDynamicBufferFromBinding",
    ()=>getDynamicBufferFromBinding,
    "isBufferRangeBinding",
    ()=>isBufferRangeBinding,
    "isDynamicBufferRange",
    ()=>isDynamicBufferRange,
    "resolveBufferBindingSource",
    ()=>resolveBufferBindingSource,
    "resolveBufferRangeBinding",
    ()=>resolveBufferRangeBinding,
    "resolveDynamicBufferRangeBinding",
    ()=>resolveDynamicBufferRangeBinding
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/buffer.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/uid.js [app-ssr] (ecmascript)");
;
;
const DEFAULT_MAX_DEBUG_DATA_BYTE_LENGTH = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Buffer"].DEBUG_DATA_MAX_LENGTH;
class DynamicBuffer {
    /** Device that owns the backing buffer. */ device;
    /** Application-provided or generated identifier. */ id;
    /** Ready promise provided for compatibility with other dynamic resources. */ ready;
    /** Usage flags applied to every backing buffer created by this wrapper. */ usage;
    /** Normalized buffer props reused when the backing buffer is recreated. */ props;
    /** Dynamic buffers are synchronously ready after construction. */ isReady = true;
    /** Whether {@link destroy} has been called. */ destroyed = false;
    /** Monotonic version that increments whenever the backing buffer is replaced. */ generation = 0;
    /** Last update timestamp for writes, reads that populate debug data, or resize operations. */ updateTimestamp;
    /** Optional CPU-side mirror of recent writes and readbacks for debugging. */ debugData = new ArrayBuffer(0);
    _debugDataEnabled;
    _maxDebugDataByteLength;
    _ownsBuffer;
    _buffer;
    /** Current immutable core buffer backing this dynamic wrapper. */ get buffer() {
        return this._buffer;
    }
    /** Current byte length of the backing buffer. */ get byteLength() {
        return this._buffer.byteLength;
    }
    /** String tag used by `Object.prototype.toString`. */ get [Symbol.toStringTag]() {
        return 'DynamicBuffer';
    }
    /** Human-readable debug string. */ toString() {
        return `DynamicBuffer:"${this.id}":${this.byteLength}B`;
    }
    /** Compact serialization for assertion diffs and structured debug logs. */ toJSON() {
        return this.toString();
    }
    /** Creates a dynamic buffer and its initial backing {@link Buffer}. */ constructor(device, props){
        const { debugData: debugDataProps = false, buffer: adoptedBuffer, ownsBuffer = true, ...bufferProps } = props;
        if (adoptedBuffer && adoptedBuffer.device !== device) {
            throw new Error('DynamicBuffer adopted buffers must belong to the supplied device');
        }
        if (adoptedBuffer && (bufferProps.byteLength !== undefined || bufferProps.data !== undefined)) {
            throw new Error('DynamicBuffer cannot combine an adopted buffer with byteLength or data');
        }
        const id = props.id || adoptedBuffer?.id || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["uid"])('dynamic-buffer');
        const normalizedBufferProps = {
            ...bufferProps,
            id,
            usage: bufferProps.usage ?? adoptedBuffer?.usage,
            indexType: bufferProps.indexType ?? adoptedBuffer?.indexType
        };
        if ((normalizedBufferProps.usage || 0) & __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Buffer"].INDEX && !normalizedBufferProps.indexType) {
            if (bufferProps.data instanceof Uint32Array) {
                normalizedBufferProps.indexType = 'uint32';
            } else if (bufferProps.data instanceof Uint16Array) {
                normalizedBufferProps.indexType = 'uint16';
            } else if (bufferProps.data instanceof Uint8Array) {
                normalizedBufferProps.indexType = 'uint8';
            }
        }
        delete normalizedBufferProps.data;
        delete normalizedBufferProps.byteOffset;
        this.device = device;
        this.id = id;
        this.props = normalizedBufferProps;
        this.usage = normalizedBufferProps.usage || 0;
        this._debugDataEnabled = Boolean(debugDataProps);
        this._maxDebugDataByteLength = typeof debugDataProps === 'object' && debugDataProps.maxByteLength !== undefined ? debugDataProps.maxByteLength : DEFAULT_MAX_DEBUG_DATA_BYTE_LENGTH;
        this._ownsBuffer = ownsBuffer;
        this._buffer = adoptedBuffer ?? this.device.createBuffer({
            ...bufferProps,
            id
        });
        this.ready = Promise.resolve(this._buffer);
        this.updateTimestamp = this._buffer.updateTimestamp;
        this._resetDebugData(this._buffer.byteLength);
        if (bufferProps.data) {
            this._writeDebugData(bufferProps.data, bufferProps.byteOffset || 0);
        }
    }
    /**
     * Writes bytes into the current backing buffer.
     *
     * @param data - Bytes or typed-array view to upload.
     * @param byteOffset - Destination byte offset in the backing buffer.
     */ write(data, byteOffset = 0) {
        this._buffer.write(data, byteOffset);
        this._touch();
        this._writeDebugData(data, byteOffset);
    }
    /**
     * Maps the current backing buffer for writing and mirrors the written range when debug data is enabled.
     *
     * @param callback - Callback invoked with the mapped range.
     * @param byteOffset - Byte offset of the mapped range.
     * @param byteLength - Byte length of the mapped range.
     */ async mapAndWriteAsync(callback, byteOffset = 0, byteLength = this.byteLength - byteOffset) {
        let copiedBytes = null;
        await this._buffer.mapAndWriteAsync(async (arrayBuffer, lifetime)=>{
            await callback(arrayBuffer, lifetime);
            copiedBytes = new Uint8Array(arrayBuffer.slice(0, byteLength));
        }, byteOffset, byteLength);
        this._touch();
        if (copiedBytes) {
            this._writeDebugData(copiedBytes, byteOffset);
        }
    }
    /**
     * Reads bytes from the current backing buffer.
     *
     * @param byteOffset - Source byte offset in the backing buffer.
     * @param byteLength - Number of bytes to read.
     */ async readAsync(byteOffset = 0, byteLength = this.byteLength - byteOffset) {
        const data = await this._buffer.readAsync(byteOffset, byteLength);
        if (this._writeDebugData(data, byteOffset)) {
            this._touch();
        }
        return data;
    }
    /**
     * Maps the current backing buffer for reading.
     *
     * @param callback - Callback invoked with the mapped range.
     * @param byteOffset - Byte offset of the mapped range.
     * @param byteLength - Byte length of the mapped range.
     */ async mapAndReadAsync(callback, byteOffset = 0, byteLength = this.byteLength - byteOffset) {
        let copiedBytes = null;
        const result = await this._buffer.mapAndReadAsync(async (arrayBuffer, lifetime)=>{
            copiedBytes = new Uint8Array(arrayBuffer.slice(0));
            return await callback(arrayBuffer, lifetime);
        }, byteOffset, byteLength);
        if (copiedBytes && this._writeDebugData(copiedBytes, byteOffset)) {
            this._touch();
        }
        return result;
    }
    /**
     * Replaces the backing buffer with a new size.
     *
     * @param options.byteLength - New backing buffer byte length.
     * @param options.preserveData - Copies bytes from the old buffer into the new buffer.
     * @param options.copyByteLength - Maximum number of bytes to copy when preserving data.
     * @returns `true` when a new backing buffer was created.
     */ resize(options) {
        const { byteLength, preserveData = false } = options;
        if (byteLength === this.byteLength) {
            return false;
        }
        const copyByteLength = Math.min(options.copyByteLength ?? Math.min(this.byteLength, byteLength), this.byteLength, byteLength);
        const previousBuffer = this._buffer;
        const previousDebugData = this.debugData.slice(0);
        const { data: _initialData, byteOffset: _initialByteOffset, ...resizableBufferProps } = this.props;
        const nextBuffer = this.device.createBuffer({
            ...resizableBufferProps,
            byteLength
        });
        if (preserveData && copyByteLength > 0) {
            this._copyBufferContents(previousBuffer, nextBuffer, copyByteLength);
        }
        this._buffer = nextBuffer;
        this._resetDebugData(byteLength);
        if (preserveData && previousDebugData.byteLength > 0) {
            this._writeDebugData(previousDebugData, 0);
        }
        if (this._ownsBuffer) {
            previousBuffer.destroy();
        }
        this._ownsBuffer = true;
        this.generation++;
        this._touch();
        return true;
    }
    /**
     * Grows the backing buffer when the requested size exceeds the current size.
     *
     * @param byteLength - Minimum required byte length.
     * @param options.preserveData - Copies existing bytes when growth is required.
     * @returns `true` when the backing buffer grew.
     */ ensureSize(byteLength, options) {
        if (byteLength <= this.byteLength) {
            return false;
        }
        return this.resize({
            byteLength,
            preserveData: options?.preserveData
        });
    }
    /**
     * Returns the current backing buffer or a range binding over it.
     *
     * @param range - Optional byte range for uniform/storage buffer bindings.
     */ getBinding(range) {
        if (range?.offset === undefined && range?.size === undefined) {
            return this._buffer;
        }
        return {
            buffer: this._buffer,
            offset: range?.offset,
            size: range?.size
        };
    }
    /** Destroys the current backing buffer and clears debug data. */ destroy() {
        if (!this.destroyed) {
            if (this._ownsBuffer) {
                this._buffer.destroy();
            }
            this.destroyed = true;
            this.debugData = new ArrayBuffer(0);
        }
    }
    _copyBufferContents(sourceBuffer, destinationBuffer, byteLength) {
        const copyByteLength = this.device.type === 'webgpu' ? Math.ceil(byteLength / 4) * 4 : byteLength;
        const commandEncoder = this.device.createCommandEncoder();
        commandEncoder.copyBufferToBuffer({
            sourceBuffer,
            destinationBuffer,
            size: copyByteLength
        });
        this.device.submit(commandEncoder.finish());
    }
    _touch() {
        this.updateTimestamp = this.device.incrementTimestamp();
    }
    _resetDebugData(byteLength) {
        if (!this._debugDataEnabled) {
            this.debugData = new ArrayBuffer(0);
            return;
        }
        this.debugData = new ArrayBuffer(Math.min(byteLength, this._maxDebugDataByteLength));
    }
    _writeDebugData(data, byteOffset) {
        if (!this._debugDataEnabled || this.debugData.byteLength === 0 || byteOffset >= this.debugData.byteLength) {
            return false;
        }
        const source = ArrayBuffer.isView(data) ? new Uint8Array(data.buffer, data.byteOffset, data.byteLength) : new Uint8Array(data);
        const target = new Uint8Array(this.debugData);
        const copyByteLength = Math.min(source.byteLength, target.byteLength - byteOffset);
        target.set(source.subarray(0, copyByteLength), byteOffset);
        return copyByteLength > 0;
    }
}
function isBufferRangeBinding(binding) {
    return binding !== null && typeof binding === 'object' && 'buffer' in binding;
}
function isDynamicBufferRange(binding) {
    return isBufferRangeBinding(binding) && binding.buffer instanceof DynamicBuffer;
}
function getDynamicBufferFromBinding(binding) {
    if (binding instanceof DynamicBuffer) {
        return binding;
    }
    if (isBufferRangeBinding(binding) && binding.buffer instanceof DynamicBuffer) {
        return binding.buffer;
    }
    return null;
}
function resolveBufferBindingSource(buffer) {
    return buffer instanceof DynamicBuffer ? buffer.buffer : buffer;
}
function resolveDynamicBufferRangeBinding(binding) {
    return resolveBufferRangeBinding(binding);
}
function resolveBufferRangeBinding(binding) {
    return {
        buffer: resolveBufferBindingSource(binding.buffer),
        offset: binding.offset,
        size: binding.size
    };
} //# sourceMappingURL=dynamic-buffer.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/model/split-uniforms-and-bindings.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "isUniformValue",
    ()=>isUniformValue,
    "splitUniformsAndBindings",
    ()=>splitUniformsAndBindings
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$math$2e$gl$2f$types$2f$dist$2f$is$2d$array$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@math.gl/types/dist/is-array.js [app-ssr] (ecmascript)");
;
function isUniformValue(value) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$math$2e$gl$2f$types$2f$dist$2f$is$2d$array$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["isNumericArray"])(value) || typeof value === 'number' || typeof value === 'boolean';
}
function splitUniformsAndBindings(uniforms, uniformTypes = {}) {
    const result = {
        bindings: {},
        uniforms: {}
    };
    Object.keys(uniforms).forEach((name)=>{
        const uniform = uniforms[name];
        if (Object.prototype.hasOwnProperty.call(uniformTypes, name) || isUniformValue(uniform)) {
            result.uniforms[name] = uniform;
        } else {
            result.bindings[name] = uniform;
        }
    });
    return result;
} //# sourceMappingURL=split-uniforms-and-bindings.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/shader-inputs.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "ShaderInputs",
    ()=>ShaderInputs
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/utils/log.js [app-ssr] (ecmascript)");
// import type {VariableShaderType, UniformValue, UniformFormat, UniformInfoDevice, Texture, Sampler} from '@luma.gl/core';
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$module$2f$shader$2d$module$2d$dependencies$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/shadertools/dist/lib/shader-module/shader-module-dependencies.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$model$2f$split$2d$uniforms$2d$and$2d$bindings$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/model/split-uniforms-and-bindings.js [app-ssr] (ecmascript)");
;
;
;
class ShaderInputs {
    options = {
        disableWarnings: false
    };
    /** Registered shader modules, including resolved dependencies. */ // @ts-ignore Fix typings
    modules;
    /** Stores the uniform values for each module */ moduleUniforms;
    /** Stores resource bindings for each module. */ moduleBindings;
    /** Stores shader resource bindings that are not owned by a shader module. */ directBindings = {};
    /** Tracks if uniforms have changed */ // moduleUniformsChanged: Record<keyof ShaderPropsT, false | string>;
    /**
     * Creates shader input storage for one or more shader modules.
     * @param modules Initial shader modules keyed by module name.
     * @param options Shader input behavior options.
     */ constructor(// @ts-ignore Fix typings
    modules, options){
        Object.assign(this.options, options);
        // Extract modules with dependencies
        const resolvedModules = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$module$2f$shader$2d$module$2d$dependencies$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getShaderModuleDependencies"])(Object.values(modules).filter(isShaderInputsModuleWithDependencies));
        for (const resolvedModule of resolvedModules){
            // @ts-ignore
            modules[resolvedModule.name] = resolvedModule;
        }
        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].log(1, 'Creating ShaderInputs with modules', Object.keys(modules))();
        // Store the module definitions and create storage for uniform values and binding values, per module
        // @ts-ignore Fix typings
        this.modules = modules;
        this.moduleUniforms = {};
        this.moduleBindings = {};
        // Initialize the modules
        for (const [name, module] of Object.entries(modules)){
            if (module) {
                this._addModule(module);
                if (module.name && name !== module.name && !this.options.disableWarnings) {
                    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].warn(`Module name: ${name} vs ${module.name}`)();
                }
            }
        }
    }
    /** Destroy */ destroy() {}
    /**
     * Set module props
     */ setProps(props) {
        if (props.bindings) {
            Object.assign(this.directBindings, props.bindings);
        }
        for (const name of Object.keys(props)){
            if (name === 'bindings') {
                continue;
            }
            const moduleName = name;
            const moduleProps = props[moduleName] || {};
            const module = this.modules[moduleName];
            if (!module) {
                // Ignore props for unregistered modules
                if (!this.options.disableWarnings) {
                    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].warn(`Module ${name} not found`)();
                }
            } else {
                const oldUniforms = this.moduleUniforms[moduleName];
                const oldBindings = this.moduleBindings[moduleName];
                const uniformsAndBindings = module.getUniforms?.(moduleProps, oldUniforms) || moduleProps;
                const { uniforms, bindings } = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$model$2f$split$2d$uniforms$2d$and$2d$bindings$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["splitUniformsAndBindings"])(uniformsAndBindings, module.uniformTypes);
                this.moduleUniforms[moduleName] = mergeModuleUniforms(oldUniforms, uniforms, module.uniformTypes);
                this.moduleBindings[moduleName] = {
                    ...oldBindings,
                    ...bindings
                };
            // this.moduleUniformsChanged ||= moduleName;
            }
        // console.log(`setProps(${String(moduleName)}`, moduleName, this.moduleUniforms[moduleName])
        }
    }
    /** Returns registered shader modules, including resolved dependencies. */ getModules() {
        return Object.values(this.modules);
    }
    /**
     * Adds shader modules that should participate in subsequent uniform and binding updates.
     * Module dependencies are resolved before new module state is initialized.
     * @param modules Shader modules to register after construction.
     */ addModules(modules) {
        const modulesToAdd = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$module$2f$shader$2d$module$2d$dependencies$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getShaderModuleDependencies"])(modules);
        for (const module of modulesToAdd){
            const moduleName = module.name;
            if (!this.modules[moduleName]) {
                // @ts-ignore The ShaderInputs module registry is intentionally extended at runtime.
                this.modules[moduleName] = module;
                this._addModule(module);
            }
        }
    }
    /** Get all uniform values for all modules */ getUniformValues() {
        return this.moduleUniforms;
    }
    /** Merges concrete and deferred bindings for the shader from the registered modules. */ getBindingValues() {
        const bindings = {};
        for (const moduleBindings of Object.values(this.moduleBindings)){
            Object.assign(bindings, moduleBindings);
        }
        Object.assign(bindings, this.directBindings);
        return bindings;
    }
    /**
     * Gets binding values for a single shader module without flattening them into the full shader map.
     *
     * This is used by systems such as {@link ShaderPassRenderer} that execute one shader module at a
     * time and need the defaults for just that module.
     */ getModuleBindingValues(moduleName) {
        const moduleBindings = this.moduleBindings[moduleName];
        return moduleBindings ? {
            ...moduleBindings
        } : {};
    }
    // INTERNAL
    /** Return a debug table that can be used for console.table() or log.table() */ getDebugTable() {
        const table = {};
        for (const [moduleName, module] of Object.entries(this.moduleUniforms)){
            for (const [key, value] of Object.entries(module)){
                table[`${moduleName}.${key}`] = {
                    type: this.modules[moduleName].uniformTypes?.[key],
                    value: String(value)
                };
            }
        }
        return table;
    }
    _addModule(module) {
        const moduleName = module.name;
        // Get default uniforms from module
        this.moduleUniforms[moduleName] = mergeModuleUniforms({}, module.defaultUniforms || {}, module.uniformTypes);
        this.moduleBindings[moduleName] = {};
    }
}
function mergeModuleUniforms(currentUniforms = {}, nextUniforms = {}, uniformTypes = {}) {
    const mergedUniforms = {
        ...currentUniforms
    };
    for (const [key, value] of Object.entries(nextUniforms)){
        if (value !== undefined) {
            mergedUniforms[key] = mergeModuleUniformValue(currentUniforms[key], value, uniformTypes[key]);
        }
    }
    return mergedUniforms;
}
function mergeModuleUniformValue(currentValue, nextValue, uniformType) {
    if (!uniformType || typeof uniformType === 'string') {
        return cloneModuleUniformValue(nextValue);
    }
    if (Array.isArray(uniformType)) {
        if (isPackedUniformArrayValue(nextValue) || !Array.isArray(nextValue)) {
            return cloneModuleUniformValue(nextValue);
        }
        const currentArray = Array.isArray(currentValue) && !isPackedUniformArrayValue(currentValue) ? [
            ...currentValue
        ] : [];
        const mergedArray = currentArray.slice();
        for(let index = 0; index < nextValue.length; index++){
            const elementValue = nextValue[index];
            if (elementValue !== undefined) {
                mergedArray[index] = mergeModuleUniformValue(currentArray[index], elementValue, uniformType[0]);
            }
        }
        return mergedArray;
    }
    if (!isPlainUniformObject(nextValue)) {
        return cloneModuleUniformValue(nextValue);
    }
    const uniformStruct = uniformType;
    const currentObject = isPlainUniformObject(currentValue) ? currentValue : {};
    const mergedObject = {
        ...currentObject
    };
    for (const [key, value] of Object.entries(nextValue)){
        if (value !== undefined) {
            mergedObject[key] = mergeModuleUniformValue(currentObject[key], value, uniformStruct[key]);
        }
    }
    return mergedObject;
}
function cloneModuleUniformValue(value) {
    if (ArrayBuffer.isView(value)) {
        return Array.prototype.slice.call(value);
    }
    if (Array.isArray(value)) {
        if (isPackedUniformArrayValue(value)) {
            return value.slice();
        }
        const compositeArray = value;
        return compositeArray.map((element)=>element === undefined ? undefined : cloneModuleUniformValue(element));
    }
    if (isPlainUniformObject(value)) {
        return Object.fromEntries(Object.entries(value).map(([key, nestedValue])=>[
                key,
                nestedValue === undefined ? undefined : cloneModuleUniformValue(nestedValue)
            ]));
    }
    return value;
}
function isPackedUniformArrayValue(value) {
    return ArrayBuffer.isView(value) || Array.isArray(value) && (value.length === 0 || typeof value[0] === 'number');
}
function isPlainUniformObject(value) {
    return Boolean(value) && typeof value === 'object' && !Array.isArray(value) && !ArrayBuffer.isView(value);
}
function isShaderInputsModuleWithDependencies(module) {
    return Boolean(module?.dependencies);
} //# sourceMappingURL=shader-inputs.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/utils/shader-module-utils.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "getShaderModuleUniformBlockLayouts",
    ()=>getShaderModuleUniformBlockLayouts,
    "mergeInferredShaderLayout",
    ()=>mergeInferredShaderLayout,
    "mergeShaderModuleBindingsIntoLayout",
    ()=>mergeShaderModuleBindingsIntoLayout,
    "mergeShaderModules",
    ()=>mergeShaderModules,
    "shaderModuleHasUniforms",
    ()=>shaderModuleHasUniforms
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$module$2f$shader$2d$module$2d$uniform$2d$layout$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/shadertools/dist/lib/shader-module/shader-module-uniform-layout.js [app-ssr] (ecmascript)");
;
function mergeShaderModuleBindingsIntoLayout(shaderLayout, modules) {
    if (!shaderLayout || !modules.some((module)=>module.bindingLayout?.length)) {
        return shaderLayout;
    }
    const mergedLayout = {
        ...shaderLayout,
        bindings: shaderLayout.bindings.map((binding)=>({
                ...binding
            }))
    };
    if ('attributes' in (shaderLayout || {})) {
        mergedLayout.attributes = shaderLayout?.attributes || [];
    }
    for (const module of modules){
        for (const bindingLayout of module.bindingLayout || []){
            for (const relatedBindingName of getRelatedBindingNames(bindingLayout.name)){
                const binding = mergedLayout.bindings.find((candidate)=>candidate.name === relatedBindingName);
                if (binding?.group === 0) {
                    binding.group = bindingLayout.group;
                }
                if (binding && bindingLayout.visibility !== undefined) {
                    binding.visibility = bindingLayout.visibility;
                }
            }
        }
    }
    return mergedLayout;
}
function mergeInferredShaderLayout(shaderLayout, inferredShaderLayout, inferredAttributeNames = []) {
    if (!shaderLayout) {
        return inferredShaderLayout;
    }
    if (!inferredShaderLayout) {
        return shaderLayout;
    }
    return {
        ...shaderLayout,
        attributes: shaderLayout.attributes.length ? mergeAttributeLayouts(shaderLayout.attributes, inferredShaderLayout.attributes.filter((attribute)=>inferredAttributeNames.includes(attribute.name))) : inferredShaderLayout.attributes,
        bindings: mergeBindingLayouts(shaderLayout.bindings, inferredShaderLayout.bindings)
    };
}
function shaderModuleHasUniforms(module) {
    return Boolean(module.uniformTypes && !isObjectEmpty(module.uniformTypes));
}
function getShaderModuleUniformBlockLayouts(modules) {
    const uniformBlockLayouts = [];
    for (const module of modules){
        const defaultUniformBlockName = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$module$2f$shader$2d$module$2d$uniform$2d$layout$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getShaderModuleUniformBlockName"])(module);
        const std140BlockNames = new Set([
            module.vs,
            module.fs
        ].flatMap((shaderSource)=>shaderSource ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$module$2f$shader$2d$module$2d$uniform$2d$layout$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getGLSLUniformBlocks"])(shaderSource).filter((block)=>block.isStd140).map((block)=>block.blockName) : []));
        const uniformBlockName = std140BlockNames.has(defaultUniformBlockName) ? defaultUniformBlockName : std140BlockNames.size === 1 ? std140BlockNames.values().next().value : undefined;
        if (shaderModuleHasUniforms(module) && uniformBlockName) {
            uniformBlockLayouts.push({
                name: uniformBlockName,
                uniformTypes: module.uniformTypes
            });
        }
    }
    return uniformBlockLayouts;
}
function mergeShaderModules(explicitModules, shaderInputModules) {
    const modules = [];
    const moduleNames = new Set();
    for (const module of [
        ...explicitModules || [],
        ...shaderInputModules || []
    ]){
        if (!moduleNames.has(module.name)) {
            moduleNames.add(module.name);
            modules.push(module);
        }
    }
    return modules;
}
/** Returns binding-name aliases that should share the module-declared bind group. */ function getRelatedBindingNames(bindingName) {
    const bindingNames = new Set([
        bindingName,
        `${bindingName}Uniforms`
    ]);
    if (!bindingName.endsWith('Uniforms')) {
        bindingNames.add(`${bindingName}Sampler`);
    }
    return [
        ...bindingNames
    ];
}
function isObjectEmpty(obj) {
    for(const _key in obj){
        return false;
    }
    return true;
}
function mergeBindingLayouts(explicitBindings, inferredBindings) {
    const mergedBindings = explicitBindings.map((binding)=>({
            ...binding
        }));
    const explicitBindingNames = new Set(explicitBindings.map((binding)=>binding.name));
    const explicitBindingLocations = new Set(explicitBindings.map((binding)=>`${binding.group}:${binding.location}`));
    for (const inferredBinding of inferredBindings){
        const inferredBindingLocation = `${inferredBinding.group}:${inferredBinding.location}`;
        if (!explicitBindingNames.has(inferredBinding.name) && !explicitBindingLocations.has(inferredBindingLocation)) {
            mergedBindings.push({
                ...inferredBinding
            });
        }
    }
    return mergedBindings;
}
function mergeAttributeLayouts(explicitAttributes, inferredAttributes) {
    const mergedAttributes = explicitAttributes.map((attribute)=>({
            ...attribute
        }));
    const explicitAttributesByName = new Map(explicitAttributes.map((attribute)=>[
            attribute.name,
            attribute
        ]));
    const explicitAttributesByLocation = new Map(explicitAttributes.map((attribute)=>[
            attribute.location,
            attribute
        ]));
    for (const inferredAttribute of inferredAttributes){
        const explicitByName = explicitAttributesByName.get(inferredAttribute.name);
        if (explicitByName) {
            if (explicitByName.type !== inferredAttribute.type || explicitByName.location !== inferredAttribute.location) {
                throw new Error(`Shader attribute "${inferredAttribute.name}" conflicts with its inferred type or location`);
            }
            continue;
        }
        const explicitByLocation = explicitAttributesByLocation.get(inferredAttribute.location);
        if (explicitByLocation) {
            throw new Error(`Shader attributes "${explicitByLocation.name}" and "${inferredAttribute.name}" both use location ${inferredAttribute.location}`);
        }
        mergedAttributes.push({
            ...inferredAttribute
        });
    }
    return mergedAttributes;
} //# sourceMappingURL=shader-module-utils.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/compute/computation.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "Computation",
    ()=>Computation,
    "getPlatformInfo",
    ()=>getPlatformInfo
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/buffer.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$compute$2d$pipeline$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/compute-pipeline.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$factories$2f$pipeline$2d$factory$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/factories/pipeline-factory.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$factories$2f$shader$2d$factory$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/factories/shader-factory.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$portable$2f$uniform$2d$store$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/portable/uniform-store.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$assert$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/utils/assert.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/utils/log.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$data$2d$types$2f$data$2d$type$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/shadertypes/data-types/data-type-decoder.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$plugin$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/shadertools/dist/lib/shader-plugin.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$assembler$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/shadertools/dist/lib/shader-assembler.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$math$2e$gl$2f$types$2f$dist$2f$is$2d$array$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@math.gl/types/dist/is-array.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$shader$2d$inputs$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/shader-inputs.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/shader-module-utils.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/uid.js [app-ssr] (ecmascript)");
;
;
;
;
;
;
// import {getDebugTableForShaderLayout} from '../debug/debug-shader-layout';
const LOG_DRAW_PRIORITY = 2;
const LOG_DRAW_TIMEOUT = 10000;
class Computation {
    static defaultProps = {
        ...__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$compute$2d$pipeline$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ComputePipeline"].defaultProps,
        id: 'unnamed',
        handle: undefined,
        userData: {},
        source: '',
        modules: [],
        defines: {},
        plugins: [],
        bindings: undefined,
        shaderInputs: undefined,
        pipelineFactory: undefined,
        shaderFactory: undefined,
        shaderAssembler: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$assembler$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ShaderAssembler"].getDefaultShaderAssembler('wgsl'),
        debugShaders: undefined
    };
    device;
    id;
    pipelineFactory;
    shaderFactory;
    userData = {};
    /** Bindings (textures, samplers, uniform buffers) */ bindings = {};
    /** The underlying GPU pipeline. */ pipeline;
    /** Assembled compute shader source */ source;
    /** the underlying compiled compute shader */ // @ts-ignore Set in function called from constructor
    shader;
    /** ShaderInputs instance */ shaderInputs;
    // @ts-ignore Set in function called from constructor
    _uniformStore;
    _pipelineNeedsUpdate = 'newly created';
    _getModuleUniforms;
    props;
    _destroyed = false;
    constructor(device, props){
        if (device.type !== 'webgpu') {
            throw new Error('Computation is only supported in WebGPU');
        }
        this.props = {
            ...Computation.defaultProps,
            ...props
        };
        props = this.props;
        this.id = props.id || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["uid"])('model');
        this.device = device;
        Object.assign(this.userData, props.userData);
        const platformInfo = getPlatformInfo(device);
        const resolvedPlugins = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$plugin$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["resolveShaderPlugins"])(this.props.plugins, platformInfo.shaderLanguage);
        if (Object.keys(resolvedPlugins.vertexInputs).length > 0 || Object.keys(resolvedPlugins.varyings).length > 0) {
            throw new Error('Computation does not support ShaderPlugin vertex inputs or varyings');
        }
        // Setup shader module inputs
        const shaderInputModules = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$plugin$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeShaderPluginModules"])(this.props.modules, resolvedPlugins.modules);
        const moduleMap = Object.fromEntries(shaderInputModules.map((module)=>[
                module.name,
                module
            ]));
        // @ts-ignore TODO - fix up typing?
        this.shaderInputs = props.shaderInputs || new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$shader$2d$inputs$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ShaderInputs"](moduleMap);
        if (props.shaderInputs && resolvedPlugins.modules.length > 0) {
            this.shaderInputs.addModules(resolvedPlugins.modules);
        }
        this.setShaderInputs(this.shaderInputs);
        // Setup shader assembler
        const modules = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeShaderModules"])(this.props.modules, this.shaderInputs?.getModules());
        const defines = {
            ...resolvedPlugins.defines,
            ...this.props.defines
        };
        this.props.shaderLayout = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeShaderModuleBindingsIntoLayout"])(this.props.shaderLayout, modules) || null;
        this.pipelineFactory = props.pipelineFactory || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$factories$2f$pipeline$2d$factory$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["PipelineFactory"].getDefaultPipelineFactory(this.device);
        this.shaderFactory = props.shaderFactory || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$factories$2f$shader$2d$factory$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ShaderFactory"].getDefaultShaderFactory(this.device);
        const shaderAssembler = this.props.shaderAssembler;
        // Compute shaders require an assembler with WGSL-specific hooks and binding state.
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$assert$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["assert"])(shaderAssembler instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$assembler$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["WGSLShaderAssembler"]);
        const { source, getUniforms, shaderLayout: assembledShaderLayout } = shaderAssembler.assembleWGSLShader({
            platformInfo,
            ...this.props,
            modules,
            defines,
            scanVertexAttributes: false,
            pluginInjections: resolvedPlugins.injections
        });
        this.source = source;
        // @ts-ignore
        this._getModuleUniforms = getUniforms;
        const inferredShaderLayout = assembledShaderLayout ?? device.getShaderLayout?.(this.source, {
            scanVertexAttributes: false
        });
        this.props.shaderLayout = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeShaderModuleBindingsIntoLayout"])(this.props.shaderLayout || inferredShaderLayout || null, modules) || null;
        // Create the pipeline
        // @note order is important
        this.pipeline = this._updatePipeline();
        // Apply any dynamic settings that will not trigger pipeline change
        if (props.bindings) {
            this.setBindings(props.bindings);
        }
    }
    destroy() {
        if (this._destroyed) return;
        this.pipelineFactory.release(this.pipeline);
        this.shaderFactory.release(this.shader);
        this._uniformStore.destroy();
        this._destroyed = true;
    }
    // Draw call
    /**
     * Updates compute shader inputs before opening the compute pass that will
     * consume them.
     */ predraw(commandEncoder) {
        // Update uniform buffers if needed
        this.updateShaderInputs(commandEncoder);
    }
    dispatch(computePass, x, y, z) {
        try {
            this._logDrawCallStart();
            this._setPipeline(computePass);
            computePass.dispatch(x, y, z);
        } finally{
            this._logDrawCallEnd();
        }
    }
    /** Dispatches a GPU-written workgroup count from an indirect buffer. */ dispatchIndirect(computePass, indirectBuffer, indirectOffset = 0) {
        try {
            this._logDrawCallStart();
            this._setPipeline(computePass);
            computePass.dispatchIndirect(indirectBuffer, indirectOffset);
        } finally{
            this._logDrawCallEnd();
        }
    }
    _setPipeline(computePass) {
        // Check if the pipeline is invalidated
        // TODO - this is likely the worst place to do this from performance perspective. Perhaps add a predraw()?
        this.pipeline = this._updatePipeline();
        // Set pipeline state, we may be sharing a pipeline so we need to set all state on every draw
        // Any caching needs to be done inside the pipeline functions
        this.pipeline.setBindings(this.bindings);
        computePass.setPipeline(this.pipeline);
        // @ts-expect-error ComputePass implementations expose binding application internally.
        computePass.setBindings({});
    }
    // Update fixed fields (can trigger pipeline rebuild)
    // Update dynamic fields
    /**
     * Updates the vertex count (used in draw calls)
     * @note Any attributes with stepMode=vertex need to be at least this big
     */ setVertexCount(vertexCount) {
    // this.vertexCount = vertexCount;
    }
    /**
     * Updates the instance count (used in draw calls)
     * @note Any attributes with stepMode=instance need to be at least this big
     */ setInstanceCount(instanceCount) {
    // this.instanceCount = instanceCount;
    }
    setShaderInputs(shaderInputs) {
        this.shaderInputs = shaderInputs;
        this._uniformStore = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$portable$2f$uniform$2d$store$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["UniformStore"](this.device, this.shaderInputs.modules);
        // Create uniform buffer bindings for all modules
        for (const [moduleName, module] of Object.entries(this.shaderInputs.modules)){
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["shaderModuleHasUniforms"])(module)) {
                const uniformBuffer = this._uniformStore.getManagedUniformBuffer(moduleName);
                this.bindings[`${moduleName}Uniforms`] = uniformBuffer;
            }
        }
    }
    /**
     * Updates shader module settings (which results in uniforms being set)
     */ setShaderModuleProps(props) {
        const uniforms = this._getModuleUniforms(props);
        // Extract textures & framebuffers set by the modules
        // TODO better way to extract bindings
        const keys = Object.keys(uniforms).filter((k)=>{
            const uniform = uniforms[k];
            return !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$math$2e$gl$2f$types$2f$dist$2f$is$2d$array$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["isNumericArray"])(uniform) && typeof uniform !== 'number' && typeof uniform !== 'boolean';
        });
        const bindings = {};
        for (const k of keys){
            bindings[k] = uniforms[k];
            delete uniforms[k];
        }
    }
    /**
     * Flushes current shader-input values into the internal uniform store.
     *
     * @param commandEncoder - Optional encoder used to order uniform uploads with
     * subsequent compute commands.
     */ updateShaderInputs(commandEncoder) {
        this._uniformStore.setUniforms(this.shaderInputs.getUniformValues(), commandEncoder);
    }
    /**
     * Sets bindings (textures, samplers, uniform buffers)
     */ setBindings(bindings) {
        Object.assign(this.bindings, bindings);
    }
    _setPipelineNeedsUpdate(reason) {
        this._pipelineNeedsUpdate = this._pipelineNeedsUpdate || reason;
    }
    _updatePipeline() {
        if (this._pipelineNeedsUpdate) {
            let prevShader = null;
            if (this.pipeline) {
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].log(1, `Model ${this.id}: Recreating pipeline because "${this._pipelineNeedsUpdate}".`)();
                prevShader = this.shader;
            }
            this._pipelineNeedsUpdate = false;
            this.shader = this.shaderFactory.createShader({
                id: `${this.id}-fragment`,
                stage: 'compute',
                source: this.source,
                debugShaders: this.props.debugShaders
            });
            this.pipeline = this.pipelineFactory.createComputePipeline({
                ...this.props,
                shader: this.shader
            });
            if (prevShader) {
                this.shaderFactory.release(prevShader);
            }
        }
        return this.pipeline;
    }
    /** Throttle draw call logging */ _lastLogTime = 0;
    _logOpen = false;
    _logDrawCallStart() {
        // IF level is 4 or higher, log every frame.
        const logDrawTimeout = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].level > 3 ? 0 : LOG_DRAW_TIMEOUT;
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].level < 2 || Date.now() - this._lastLogTime < logDrawTimeout) {
            return;
        }
        this._lastLogTime = Date.now();
        this._logOpen = true;
        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].group(LOG_DRAW_PRIORITY, `>>> DRAWING MODEL ${this.id}`, {
            collapsed: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].level <= 2
        })();
    }
    _logDrawCallEnd() {
        if (this._logOpen) {
            // const shaderLayoutTable = getDebugTableForShaderLayout(this.pipeline.props.shaderLayout, this.id);
            // log.table(logLevel, attributeTable)();
            // log.table(logLevel, uniformTable)();
            // log.table(LOG_DRAW_PRIORITY, shaderLayoutTable)();
            const uniformTable = this.shaderInputs.getDebugTable();
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].table(LOG_DRAW_PRIORITY, uniformTable)();
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].groupEnd(LOG_DRAW_PRIORITY)();
            this._logOpen = false;
        }
    }
    _drawCount = 0;
    // TODO - fix typing of luma data types
    _getBufferOrConstantValues(attribute, dataType) {
        const TypedArrayConstructor = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$data$2d$types$2f$data$2d$type$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["dataTypeDecoder"].getTypedArrayConstructor(dataType);
        const typedArray = attribute instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Buffer"] ? new TypedArrayConstructor(attribute.debugData) : attribute;
        return typedArray.toString();
    }
}
function getPlatformInfo(device) {
    return {
        type: device.type,
        shaderLanguage: device.info.shadingLanguage,
        shaderLanguageVersion: device.info.shadingLanguageVersion,
        gpu: device.info.gpu,
        limits: device.limits,
        // HACK - we pretend that the DeviceFeatures is a Set, it has a similar API
        features: device.features
    };
} //# sourceMappingURL=computation.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/geometry/geometry.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "Geometry",
    ()=>Geometry,
    "getGeometryShaderAttributeName",
    ()=>getGeometryShaderAttributeName
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$vertex$2d$types$2f$vertex$2d$format$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/shadertypes/vertex-types/vertex-format-decoder.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/uid.js [app-ssr] (ecmascript)");
;
;
class Geometry {
    /** Application-provided or generated identifier. */ id;
    /** Determines how vertices are assembled into primitives. */ topology;
    /** Resolved draw vertex count. */ vertexCount;
    /** Optional index attribute. */ indices;
    /** CPU attributes, keyed by caller-provided source attribute name. */ attributes;
    /** Buffer layout matching the geometry attributes. Always populated. */ bufferLayout;
    /** Application-owned metadata. */ userData = {};
    /** Creates a CPU geometry and wraps raw typed arrays into attribute records. */ constructor(props){
        const { attributes = {}, indices = null, vertexCount = null } = props;
        this.id = props.id || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["uid"])('geometry');
        this.topology = props.topology;
        if (indices) {
            this.indices = ArrayBuffer.isView(indices) ? {
                value: indices,
                size: 1
            } : indices;
        }
        this.attributes = {};
        for (const [attributeName, attributeValue] of Object.entries(attributes)){
            // Wrap "unwrapped" arrays and try to autodetect their type
            const attribute = ArrayBuffer.isView(attributeValue) ? {
                value: attributeValue
            } : attributeValue;
            if (!ArrayBuffer.isView(attribute.value)) {
                throw new Error(`${this._print(attributeName)}: must be typed array or object with value as typed array`);
            }
            if ((attributeName === 'POSITION' || attributeName === 'positions') && !attribute.size) {
                attribute.size = 3;
            }
            // Move indices to separate field
            if (attributeName === 'indices') {
                if (this.indices) {
                    throw new Error('Multiple indices detected');
                }
                this.indices = attribute;
            } else {
                const shaderAttributeName = getGeometryShaderAttributeName(attributeName);
                const storedAttributeName = Object.keys(this.attributes).find((name)=>getGeometryShaderAttributeName(name) === shaderAttributeName);
                if (storedAttributeName) {
                    delete this.attributes[storedAttributeName];
                }
                this.attributes[attributeName] = attribute;
            }
        }
        if (this.indices && this.indices['isIndexed'] !== undefined) {
            this.indices = Object.assign({}, this.indices);
            delete this.indices['isIndexed'];
        }
        this.vertexCount = vertexCount || this._calculateVertexCount(this.attributes, this.indices);
        this.bufferLayout = props.bufferLayout || getBufferLayoutFromGeometryAttributes(this.attributes);
    }
    /** Returns the resolved draw vertex count. */ getVertexCount() {
        return this.vertexCount;
    }
    /** Returns all attributes, including `indices` when index data is present. */ getAttributes() {
        return this.indices ? {
            indices: this.indices,
            ...this.attributes
        } : this.attributes;
    }
    // PRIVATE
    _print(attributeName) {
        return `Geometry ${this.id} attribute ${attributeName}`;
    }
    _setAttributes(attributes, indices) {
        return this;
    }
    _calculateVertexCount(attributes, indices) {
        if (indices) {
            return indices.value.length;
        }
        let vertexCount = Infinity;
        for (const attribute of Object.values(attributes)){
            if (!attribute) {
                continue; // eslint-disable-line no-continue
            }
            const { value, size, constant } = attribute;
            if (!constant && value && size !== undefined && size >= 1) {
                vertexCount = Math.min(vertexCount, value.length / size);
            }
        }
        // assert(Number.isFinite(vertexCount));
        return vertexCount;
    }
}
function getGeometryShaderAttributeName(attributeName) {
    switch(attributeName){
        case 'POSITION':
            return 'positions';
        case 'NORMAL':
            return 'normals';
        case 'TEXCOORD_0':
            return 'texCoords';
        case 'TEXCOORD_1':
            return 'texCoords1';
        case 'COLOR_0':
            return 'colors';
        default:
            return attributeName;
    }
}
function getBufferLayoutFromGeometryAttributes(attributes) {
    const bufferLayout = [];
    for (const [attributeName, attribute] of Object.entries(attributes)){
        if (!attribute) {
            continue; // eslint-disable-line no-continue
        }
        const { value, size, normalized } = attribute;
        if (size === undefined) {
            throw new Error(`Attribute ${attributeName} is missing a size`);
        }
        bufferLayout.push({
            name: getGeometryShaderAttributeName(attributeName),
            format: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$vertex$2d$types$2f$vertex$2d$format$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["vertexFormatDecoder"].getVertexFormatFromAttribute(value, size, normalized)
        });
    }
    return bufferLayout;
} //# sourceMappingURL=geometry.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/geometry/geometry-utils.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "makeInterleavedGeometry",
    ()=>makeInterleavedGeometry,
    "unpackIndexedGeometry",
    ()=>unpackIndexedGeometry
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$vertex$2d$types$2f$vertex$2d$format$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/shadertypes/vertex-types/vertex-format-decoder.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$geometry$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/geometry/geometry.js [app-ssr] (ecmascript)");
;
;
function unpackIndexedGeometry(geometry) {
    const { indices, attributes } = geometry;
    if (!indices) {
        return geometry;
    }
    const vertexCount = indices.value.length;
    const unpackedAttributes = {};
    for(const attributeName in attributes){
        const attribute = attributes[attributeName];
        if (!attribute) {
            continue; // eslint-disable-line
        }
        const { value, size } = attribute;
        const constant = attribute['constant'];
        if (constant || !size) {
            continue; // eslint-disable-line
        }
        const ArrayType = value.constructor;
        const unpackedValue = new ArrayType(vertexCount * size);
        for(let x = 0; x < vertexCount; ++x){
            const index = indices.value[x];
            for(let i = 0; i < size; i++){
                unpackedValue[x * size + i] = value[index * size + i];
            }
        }
        unpackedAttributes[attributeName] = {
            size,
            value: unpackedValue
        };
    }
    return {
        attributes: Object.assign({}, attributes, unpackedAttributes)
    };
}
function makeInterleavedGeometry(geometry, options = {}) {
    const bufferName = options.bufferName || 'geometry';
    if (isInterleavedGeometry(geometry, bufferName)) {
        return geometry;
    }
    const minAttributeAlignment = options.minAttributeAlignment || 4;
    const sourceAttributes = getInterleavedSourceAttributes(geometry, options.attributes);
    const interleavedAttributes = [];
    let byteOffset = 0;
    let attributeVertexCount = Infinity;
    for (const [sourceName, attribute] of sourceAttributes){
        if (!attribute) {
            continue; // eslint-disable-line no-continue
        }
        if (attribute['constant']) {
            throw new Error(`Attribute ${sourceName} is constant`);
        }
        const { value, size, normalized } = attribute;
        if (!ArrayBuffer.isView(value)) {
            throw new Error(`Attribute ${sourceName} is missing typed array data`);
        }
        if (size === undefined) {
            throw new Error(`Attribute ${sourceName} is missing a size`);
        }
        const format = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$vertex$2d$types$2f$vertex$2d$format$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["vertexFormatDecoder"].getVertexFormatFromAttribute(value, size, normalized);
        const vertexFormatInfo = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$vertex$2d$types$2f$vertex$2d$format$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["vertexFormatDecoder"].getVertexFormatInfo(format);
        byteOffset = alignTo(byteOffset, minAttributeAlignment);
        interleavedAttributes.push({
            sourceName,
            attributeName: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$geometry$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getGeometryShaderAttributeName"])(sourceName),
            value,
            size,
            format,
            byteOffset,
            byteLength: vertexFormatInfo.byteLength
        });
        byteOffset += vertexFormatInfo.byteLength;
        const sourceVertexCount = value.length / size;
        if (!Number.isInteger(sourceVertexCount)) {
            throw new Error(`Attribute ${sourceName} length is not divisible by size`);
        }
        attributeVertexCount = Math.min(attributeVertexCount, sourceVertexCount);
    }
    if (interleavedAttributes.length === 0 || !Number.isFinite(attributeVertexCount)) {
        throw new Error(`Geometry ${geometry.id} has no interleavable attributes`);
    }
    const byteStride = alignTo(byteOffset, minAttributeAlignment);
    const arrayBuffer = new ArrayBuffer(attributeVertexCount * byteStride);
    for (const attribute of interleavedAttributes){
        writeInterleavedAttribute(arrayBuffer, attributeVertexCount, byteStride, attribute);
    }
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$geometry$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Geometry"]({
        id: geometry.id,
        topology: geometry.topology || 'triangle-list',
        vertexCount: geometry.vertexCount,
        indices: geometry.indices,
        attributes: {
            [bufferName]: {
                value: new Uint8Array(arrayBuffer),
                size: byteStride,
                byteStride
            }
        },
        bufferLayout: [
            {
                name: bufferName,
                stepMode: 'vertex',
                byteStride,
                attributes: interleavedAttributes.map((attribute)=>({
                        attribute: attribute.attributeName,
                        format: attribute.format,
                        byteOffset: attribute.byteOffset
                    }))
            }
        ]
    });
}
function isInterleavedGeometry(geometry, bufferName) {
    if (geometry.bufferLayout.length !== 1) {
        return false;
    }
    const bufferLayout = geometry.bufferLayout[0];
    return bufferLayout.name === bufferName && Boolean(bufferLayout.attributes?.length) && Boolean(geometry.attributes[bufferName]);
}
function getInterleavedSourceAttributes(geometry, attributeNames) {
    if (attributeNames) {
        return attributeNames.map((attributeName)=>[
                attributeName,
                geometry.attributes[attributeName]
            ]);
    }
    return Object.entries(geometry.attributes);
}
function writeInterleavedAttribute(arrayBuffer, vertexCount, byteStride, attribute) {
    const ArrayType = attribute.value.constructor;
    const bytesPerElement = ArrayType.BYTES_PER_ELEMENT;
    if (attribute.byteOffset % bytesPerElement !== 0 || byteStride % bytesPerElement !== 0) {
        throw new Error(`Attribute ${attribute.sourceName} is not aligned to its component type`);
    }
    const target = new ArrayType(arrayBuffer);
    const source = attribute.value;
    const elementOffset = attribute.byteOffset / bytesPerElement;
    const elementStride = byteStride / bytesPerElement;
    for(let vertexIndex = 0; vertexIndex < vertexCount; vertexIndex++){
        const sourceIndex = vertexIndex * attribute.size;
        const targetIndex = vertexIndex * elementStride + elementOffset;
        for(let componentIndex = 0; componentIndex < attribute.size; componentIndex++){
            target[targetIndex + componentIndex] = source[sourceIndex + componentIndex];
        }
    }
}
function alignTo(byteOffset, alignment) {
    return Math.ceil(byteOffset / alignment) * alignment;
} // export function calculateVertexNormals(positions: Float32Array): Uint8Array {
 //   let normals = new Uint8Array(positions.length / 3);
 //   for (let i = 0; i < positions.length; i++) {
 //     const vec1 = new Vector3(positions.subarray(i * 3, i + 0, i + 3));
 //     const vec2 = new Vector3(positions.subarray(i + 3, i + 6));
 //     const vec3 = new Vector3(positions.subarray(i + 6, i + 9));
 //     const normal = new Vector3(vec1).cross(vec2).normalize();
 //     normals.set(normal[0], i + 4);
 //     normals.set(normal[1], i + 4 + 1);
 //     normals.set(normal[2], i + 2);
 //   }
 //   const normal = new Vector3(vec1).cross(vec2).normalize();
 // }
 //# sourceMappingURL=geometry-utils.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/geometry/gpu-geometry.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "GPUGeometry",
    ()=>GPUGeometry,
    "getAttributeBuffersFromGeometry",
    ()=>getAttributeBuffersFromGeometry,
    "getIndexBufferFromGeometry",
    ()=>getIndexBufferFromGeometry,
    "makeGPUGeometry",
    ()=>makeGPUGeometry
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/buffer.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$geometry$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/geometry/geometry.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$geometry$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/geometry/geometry-utils.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/uid.js [app-ssr] (ecmascript)");
;
;
;
;
class GPUGeometry {
    /** Application-provided or generated identifier. */ id;
    /** Application-owned metadata. */ userData = {};
    /** Determines how vertices are assembled into primitives. */ topology;
    /** Buffer layout matching {@link GPUGeometry.attributes}. */ bufferLayout = [];
    /** Resolved draw vertex count. */ vertexCount;
    /** Optional GPU index buffer. */ indices;
    /** GPU vertex buffers, keyed by buffer layout name. */ attributes;
    /** Creates GPU-backed geometry from existing buffers. */ constructor(props){
        this.id = props.id || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["uid"])('geometry');
        this.topology = props.topology;
        this.indices = props.indices || null;
        this.attributes = props.attributes;
        this.vertexCount = props.vertexCount;
        this.bufferLayout = props.bufferLayout || [];
        if (this.indices) {
            if (!(this.indices.usage & __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Buffer"].INDEX)) {
                throw new Error('Index buffer must have INDEX usage');
            }
        }
    }
    /** Destroys the owned index and vertex buffers. */ destroy() {
        this.indices?.destroy();
        for (const attribute of Object.values(this.attributes)){
            attribute.destroy();
        }
    }
    /** Returns the draw vertex count. */ getVertexCount() {
        return this.vertexCount;
    }
    /** Returns the GPU vertex buffers. */ getAttributes() {
        return this.attributes;
    }
    /** Returns the GPU index buffer, or `null` for non-indexed geometry. */ getIndexes() {
        return this.indices || null;
    }
    _calculateVertexCount(positions) {
        // Assume that positions is a fully packed float32x3 buffer
        const vertexCount = positions.byteLength / 12;
        return vertexCount;
    }
}
function makeGPUGeometry(device, geometry) {
    if (geometry instanceof GPUGeometry) {
        return geometry;
    }
    const sourceGeometry = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$geometry$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["makeInterleavedGeometry"])(geometry);
    const indices = getIndexBufferFromGeometry(device, sourceGeometry);
    const { attributes, bufferLayout } = getAttributeBuffersFromGeometry(device, sourceGeometry);
    return new GPUGeometry({
        topology: sourceGeometry.topology || 'triangle-list',
        bufferLayout,
        vertexCount: sourceGeometry.vertexCount,
        indices,
        attributes
    });
}
function getIndexBufferFromGeometry(device, geometry) {
    if (!geometry.indices) {
        return undefined;
    }
    const data = geometry.indices.value;
    return device.createBuffer({
        usage: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Buffer"].INDEX,
        data
    });
}
function getAttributeBuffersFromGeometry(device, geometry) {
    const attributes = {};
    for (const [attributeName, attribute] of Object.entries(geometry.attributes)){
        const name = geometry.bufferLayout.find((bufferLayout)=>bufferLayout.name === attributeName)?.name || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$geometry$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getGeometryShaderAttributeName"])(attributeName);
        if (attribute) {
            attributes[name] = device.createBuffer({
                data: attribute.value,
                id: `${attributeName}-buffer`
            });
        }
    }
    return {
        attributes,
        bufferLayout: geometry.bufferLayout,
        vertexCount: geometry.vertexCount
    };
} //# sourceMappingURL=gpu-geometry.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/debug/debug-shader-layout.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
/**
 * Extracts a table suitable for `console.table()` from a shader layout to assist in debugging.
 * @param layout shader layout
 * @param name app should provide the most meaningful name, usually the model or pipeline name / id.
 * @returns
 */ __turbopack_context__.s([
    "getDebugTableForShaderLayout",
    ()=>getDebugTableForShaderLayout
]);
function getDebugTableForShaderLayout(layout, name) {
    const table = {};
    const header = 'Values'; // '`Shader Layout for ${name}`;
    if (layout.attributes.length === 0 && !layout.varyings?.length) {
        return {
            'No attributes or varyings': {
                [header]: 'N/A'
            }
        };
    }
    for (const attributeDeclaration of layout.attributes){
        if (attributeDeclaration) {
            const glslDeclaration = `${attributeDeclaration.location} ${attributeDeclaration.name}: ${attributeDeclaration.type}`;
            table[`in ${glslDeclaration}`] = {
                [header]: attributeDeclaration.stepMode || 'vertex'
            };
        }
    }
    for (const varyingDeclaration of layout.varyings || []){
        const glslDeclaration = `${varyingDeclaration.location} ${varyingDeclaration.name}`;
        table[`out ${glslDeclaration}`] = {
            [header]: JSON.stringify(varyingDeclaration)
        };
    }
    return table;
} //# sourceMappingURL=debug-shader-layout.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/debug/debug-framebuffer.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "debugFramebuffer",
    ()=>debugFramebuffer
]);
const DEBUG_FRAMEBUFFER_STATE_KEY = '__debugFramebufferState';
const DEFAULT_MARGIN_PX = 8;
function debugFramebuffer(renderPass, source, options) {
    if (renderPass.device.type !== 'webgl') {
        return;
    }
    const state = getDebugFramebufferState(renderPass.device);
    if (state.flushing) {
        return;
    }
    if (isDefaultRenderPass(renderPass)) {
        flushDebugFramebuffers(renderPass, options, state);
        return;
    }
    if (source && isFramebuffer(source) && source.handle !== null) {
        if (!state.queuedFramebuffers.includes(source)) {
            state.queuedFramebuffers.push(source);
        }
    }
}
function flushDebugFramebuffers(renderPass, options, state) {
    if (state.queuedFramebuffers.length === 0) {
        return;
    }
    const webglDevice = renderPass.device;
    const { gl } = webglDevice;
    const previousReadFramebuffer = gl.getParameter(36010);
    const previousDrawFramebuffer = gl.getParameter(36006);
    const [targetWidth, targetHeight] = renderPass.device.getDefaultCanvasContext().getDrawingBufferSize();
    let topPx = parseCssPixel(options.top, DEFAULT_MARGIN_PX);
    const leftPx = parseCssPixel(options.left, DEFAULT_MARGIN_PX);
    state.flushing = true;
    try {
        for (const framebuffer of state.queuedFramebuffers){
            const [targetX0, targetY0, targetX1, targetY1, previewHeight] = getOverlayRect({
                framebuffer,
                targetWidth,
                targetHeight,
                topPx,
                leftPx,
                minimap: options.minimap
            });
            gl.bindFramebuffer(36008, framebuffer.handle);
            gl.bindFramebuffer(36009, null);
            gl.blitFramebuffer(0, 0, framebuffer.width, framebuffer.height, targetX0, targetY0, targetX1, targetY1, 16384, 9728);
            topPx += previewHeight + DEFAULT_MARGIN_PX;
        }
    } finally{
        gl.bindFramebuffer(36008, previousReadFramebuffer);
        gl.bindFramebuffer(36009, previousDrawFramebuffer);
        state.flushing = false;
    }
}
function getOverlayRect(options) {
    const { framebuffer, targetWidth, targetHeight, topPx, leftPx, minimap } = options;
    const maxWidth = minimap ? Math.max(Math.floor(targetWidth / 4), 1) : targetWidth;
    const maxHeight = minimap ? Math.max(Math.floor(targetHeight / 4), 1) : targetHeight;
    const scale = Math.min(maxWidth / framebuffer.width, maxHeight / framebuffer.height);
    const previewWidth = Math.max(Math.floor(framebuffer.width * scale), 1);
    const previewHeight = Math.max(Math.floor(framebuffer.height * scale), 1);
    const targetX0 = leftPx;
    const targetY0 = Math.max(targetHeight - topPx - previewHeight, 0);
    const targetX1 = targetX0 + previewWidth;
    const targetY1 = targetY0 + previewHeight;
    return [
        targetX0,
        targetY0,
        targetX1,
        targetY1,
        previewHeight
    ];
}
function getDebugFramebufferState(device) {
    device.userData[DEBUG_FRAMEBUFFER_STATE_KEY] ||= {
        flushing: false,
        queuedFramebuffers: []
    };
    return device.userData[DEBUG_FRAMEBUFFER_STATE_KEY];
}
function isFramebuffer(value) {
    return 'colorAttachments' in value;
}
function isDefaultRenderPass(renderPass) {
    const framebuffer = renderPass.props.framebuffer;
    return !framebuffer || framebuffer.handle === null;
}
function parseCssPixel(value, defaultValue) {
    if (!value) {
        return defaultValue;
    }
    const parsedValue = Number.parseInt(value, 10);
    return Number.isFinite(parsedValue) ? parsedValue : defaultValue;
} //# sourceMappingURL=debug-framebuffer.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/utils/deep-equal.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
/**
 * Fast partial deep equal for prop.
 *
 * @param a Prop
 * @param b Prop to compare against `a`
 * @param depth Depth to which to recurse in nested Objects/Arrays. Use 0 (default) for shallow comparison, -1 for infinite depth
 */ /* eslint-disable complexity */ __turbopack_context__.s([
    "deepEqual",
    ()=>deepEqual
]);
function deepEqual(a, b, depth) {
    if (a === b) {
        return true;
    }
    if (!depth || !a || !b) {
        return false;
    }
    if (Array.isArray(a)) {
        if (!Array.isArray(b) || a.length !== b.length) {
            return false;
        }
        for(let i = 0; i < a.length; i++){
            if (!deepEqual(a[i], b[i], depth - 1)) {
                return false;
            }
        }
        return true;
    }
    if (Array.isArray(b)) {
        return false;
    }
    if (typeof a === 'object' && typeof b === 'object') {
        const aKeys = Object.keys(a);
        const bKeys = Object.keys(b);
        if (aKeys.length !== bKeys.length) {
            return false;
        }
        for (const key of aKeys){
            if (!b.hasOwnProperty(key)) {
                return false;
            }
            if (!deepEqual(a[key], b[key], depth - 1)) {
                return false;
            }
        }
        return true;
    }
    return false;
} //# sourceMappingURL=deep-equal.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/utils/buffer-layout-helper.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "BufferLayoutHelper",
    ()=>BufferLayoutHelper
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$buffer$2d$layout$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter-utils/buffer-layout-utils.js [app-ssr] (ecmascript)");
;
class BufferLayoutHelper {
    bufferLayouts;
    constructor(bufferLayouts){
        this.bufferLayouts = bufferLayouts;
    }
    getBufferLayout(name) {
        return this.bufferLayouts.find((layout)=>layout.name === name) || null;
    }
    /** Get attribute names from a BufferLayout */ getAttributeNamesForBuffer(bufferLayout) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$buffer$2d$layout$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getBufferLayoutAttributeNames"])(bufferLayout);
    }
    mergeBufferLayouts(bufferLayouts1, bufferLayouts2) {
        const mergedLayouts = [
            ...bufferLayouts1
        ];
        for (const attribute of bufferLayouts2){
            const index = mergedLayouts.findIndex((attribute2)=>attribute2.name === attribute.name);
            if (index < 0) {
                mergedLayouts.push(attribute);
            } else {
                mergedLayouts[index] = attribute;
            }
        }
        return mergedLayouts;
    }
} //# sourceMappingURL=buffer-layout-helper.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/utils/buffer-layout-order.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "sortedBufferLayoutByShaderSourceLocations",
    ()=>sortedBufferLayoutByShaderSourceLocations
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$buffer$2d$layout$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter-utils/buffer-layout-utils.js [app-ssr] (ecmascript)");
;
function sortedBufferLayoutByShaderSourceLocations(shaderLayout, bufferLayout) {
    const shaderLayoutMap = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$buffer$2d$layout$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getShaderAttributeLocationMap"])(shaderLayout);
    const sortedLayout = bufferLayout.slice();
    sortedLayout.sort((a, b)=>{
        const minLocationA = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$buffer$2d$layout$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getMinimumAttributeLocation"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$buffer$2d$layout$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getBufferLayoutAttributeNames"])(a).map((attributeName)=>shaderLayoutMap[attributeName]));
        const minLocationB = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$buffer$2d$layout$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getMinimumAttributeLocation"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$buffer$2d$layout$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getBufferLayoutAttributeNames"])(b).map((attributeName)=>shaderLayoutMap[attributeName]));
        return minLocationA - minLocationB;
    });
    return sortedLayout;
} //# sourceMappingURL=buffer-layout-order.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/dynamic-texture/texture-binding-source.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "getTextureBindingLayout",
    ()=>getTextureBindingLayout,
    "isTextureBindingLayout",
    ()=>isTextureBindingLayout,
    "isTextureBindingSource",
    ()=>isTextureBindingSource
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$bind$2d$groups$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter-utils/bind-groups.js [app-ssr] (ecmascript)");
;
function isTextureBindingSource(value) {
    return value !== null && typeof value === 'object' && 'resolveTextureBinding' in value && typeof value.resolveTextureBinding === 'function';
}
function isTextureBindingLayout(bindingLayout) {
    return bindingLayout?.type === 'texture' || bindingLayout?.type === 'external-texture';
}
function getTextureBindingLayout(shaderLayout, bindingName, options) {
    const bindingLayout = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$bind$2d$groups$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getShaderLayoutBinding"])(shaderLayout, bindingName, {
        ignoreWarnings: true
    });
    if (isTextureBindingLayout(bindingLayout)) {
        return bindingLayout;
    }
    return shaderLayout.bindings.length === 0 && options?.fallbackGroup !== undefined ? {
        type: 'texture',
        name: bindingName,
        group: options.fallbackGroup,
        location: 0
    } : null;
} //# sourceMappingURL=texture-binding-source.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/model/model.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "Model",
    ()=>Model,
    "getPlatformInfo",
    ()=>getPlatformInfo
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/buffer.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$external$2d$texture$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/external-texture.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$texture$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/texture.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$texture$2d$view$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/texture-view.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$render$2d$pipeline$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/render-pipeline.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$factories$2f$pipeline$2d$factory$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/factories/pipeline-factory.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$factories$2f$shader$2d$factory$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/factories/shader-factory.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$portable$2f$uniform$2d$store$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/portable/uniform-store.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$assert$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/utils/assert.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/utils/log.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$data$2d$types$2f$data$2d$type$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/shadertypes/data-types/data-type-decoder.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$get$2d$attribute$2d$from$2d$layouts$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter-utils/get-attribute-from-layouts.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$bind$2d$groups$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter-utils/bind-groups.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$plugin$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/shadertools/dist/lib/shader-plugin.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$assembler$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/shadertools/dist/lib/shader-assembler.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$gpu$2d$geometry$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/geometry/gpu-geometry.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$debug$2f$debug$2d$shader$2d$layout$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/debug/debug-shader-layout.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$debug$2f$debug$2d$framebuffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/debug/debug-framebuffer.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$deep$2d$equal$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/deep-equal.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$buffer$2d$layout$2d$helper$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/buffer-layout-helper.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$buffer$2d$layout$2d$order$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/buffer-layout-order.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/shader-module-utils.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/utils/uid.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$shader$2d$inputs$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/shader-inputs.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/dynamic-buffer/dynamic-buffer.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$texture$2f$texture$2d$binding$2d$source$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/dynamic-texture/texture-binding-source.js [app-ssr] (ecmascript)");
;
;
;
;
;
;
;
;
;
;
;
;
;
const LOG_DRAW_PRIORITY = 2;
const LOG_DRAW_TIMEOUT = 10000;
const PIPELINE_INITIALIZATION_FAILED = 'render pipeline initialization failed';
const DEPTH_STENCIL_ATTACHMENT_FORMATS = [
    'stencil8',
    'depth16unorm',
    'depth24plus',
    'depth24plus-stencil8',
    'depth32float',
    'depth32float-stencil8'
];
class Model {
    static defaultProps = {
        ...__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$render$2d$pipeline$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["RenderPipeline"].defaultProps,
        source: undefined,
        vs: null,
        fs: null,
        id: 'unnamed',
        handle: undefined,
        userData: {},
        defines: {},
        modules: [],
        plugins: [],
        geometry: null,
        indexBuffer: null,
        indexCount: undefined,
        firstVertex: 0,
        firstIndex: 0,
        attributes: {},
        constantAttributes: {},
        bindings: {},
        uniforms: {},
        varyings: [],
        isInstanced: undefined,
        instanceCount: 0,
        vertexCount: 0,
        shaderInputs: undefined,
        material: undefined,
        pipelineFactory: undefined,
        shaderFactory: undefined,
        transformFeedback: undefined,
        shaderAssembler: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$assembler$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ShaderAssembler"].getDefaultShaderAssembler('glsl'),
        debugShaders: undefined,
        disableWarnings: undefined
    };
    /** Device that created this model */ device;
    /** Application provided identifier */ id;
    /** WGSL shader source when using unified shader */ // @ts-expect-error assigned in function called from constructor
    source;
    /** GLSL vertex shader source */ // @ts-expect-error assigned in function called from constructor
    vs;
    /** GLSL fragment shader source */ // @ts-expect-error assigned in function called from constructor
    fs;
    /** Factory used to create render pipelines */ pipelineFactory;
    /** Factory used to create shaders */ shaderFactory;
    /** User-supplied per-model data */ userData = {};
    // Fixed properties (change can trigger pipeline rebuild)
    /** The render pipeline GPU parameters, depth testing etc */ parameters;
    /** The primitive topology */ topology;
    /** Buffer layout */ bufferLayout;
    // Dynamic properties
    /** Use instanced rendering */ isInstanced = undefined;
    /** instance count. `undefined` means not instanced */ instanceCount = 0;
    /** Vertex count */ vertexCount;
    /** Indexed draw count override. Undefined draws the full bound index buffer. */ indexCount;
    /** First vertex byte offset for WebGL indexed draws or first vertex for non-indexed draws. */ firstVertex;
    /** First index element for WebGPU indexed draws. */ firstIndex;
    /** Index buffer */ indexBuffer = null;
    /** Buffer-valued attributes */ bufferAttributes = {};
    /** Constant-valued attributes */ constantAttributes = {};
    /** Bindings (textures, samplers, uniform buffers) */ bindings = {};
    /**
     * VertexArray
     * @note not implemented: if bufferLayout is updated, vertex array has to be rebuilt!
     * @todo - allow application to define multiple vertex arrays?
     * */ vertexArray;
    /** TransformFeedback, WebGL 2 only. */ transformFeedback = null;
    /** The underlying GPU "program". @note May be recreated if parameters change */ pipeline;
    /** ShaderInputs instance */ // @ts-expect-error Assigned in function called by constructor
    shaderInputs;
    material = null;
    // @ts-expect-error Assigned in function called by constructor
    _uniformStore;
    _attributeInfos = {};
    _gpuGeometry = null;
    props;
    _dynamicIndexBufferSource = null;
    _dynamicAttributeBufferSources = {};
    _colorAttachmentFormats;
    _depthStencilAttachmentFormat;
    _pipelineNeedsUpdate = 'newly created';
    _needsRedraw = 'initializing';
    _drawBlockedReason = false;
    _destroyed = false;
    /** "Time" of last draw. Monotonically increasing timestamp */ _lastDrawTimestamp = -1;
    _bindingTable = [];
    get [Symbol.toStringTag]() {
        return 'Model';
    }
    toString() {
        return `Model(${this.id})`;
    }
    constructor(device, props){
        const defaultShaderAssembler = Model.defaultProps.shaderAssembler;
        this.props = {
            ...Model.defaultProps,
            ...props,
            shaderAssembler: props.shaderAssembler ?? (isShaderAssemblerForLanguage(defaultShaderAssembler, device.info.shadingLanguage) ? defaultShaderAssembler : __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$assembler$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ShaderAssembler"].getDefaultShaderAssembler(device.info.shadingLanguage))
        };
        props = this.props;
        this.id = props.id || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$uid$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["uid"])('model');
        this.device = device;
        Object.assign(this.userData, props.userData);
        this.material = props.material || null;
        const platformInfo = getPlatformInfo(device);
        const resolvedPlugins = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$plugin$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["resolveShaderPlugins"])(this.props.plugins, platformInfo.shaderLanguage);
        // Setup shader module inputs
        const shaderInputModules = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$shader$2d$plugin$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeShaderPluginModules"])(this.props.modules, resolvedPlugins.modules);
        const moduleMap = Object.fromEntries(shaderInputModules.map((module)=>[
                module.name,
                module
            ]));
        const shaderInputs = props.shaderInputs || new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$shader$2d$inputs$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ShaderInputs"](moduleMap, {
            disableWarnings: this.props.disableWarnings
        });
        if (props.shaderInputs && resolvedPlugins.modules.length > 0) {
            shaderInputs.addModules(resolvedPlugins.modules);
        }
        // @ts-ignore
        this.setShaderInputs(shaderInputs);
        // Setup shader assembler
        const modules = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeShaderModules"])(this.props.modules, shaderInputs.getModules());
        const defines = {
            ...resolvedPlugins.defines,
            ...this.props.defines
        };
        if (this.device.type === 'webgl') {
            this.props._uniformBlockLayouts = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getShaderModuleUniformBlockLayouts"])(modules);
        }
        this.props.shaderLayout = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeShaderModuleBindingsIntoLayout"])(this.props.shaderLayout, modules) || null;
        const isWebGPU = this.device.type === 'webgpu';
        // WebGPU
        // TODO - hack to support unified WGSL shader
        // TODO - this is wrong, compile a single shader
        if (isWebGPU && this.props.source) {
            // WGSL
            const shaderAssembler = this.props.shaderAssembler;
            // WGSL sources require an assembler with WGSL-specific hooks and binding state.
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$assert$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["assert"])(isShaderAssemblerForLanguage(shaderAssembler, 'wgsl'));
            const { source, getUniforms, bindingTable, shaderLayout: assembledShaderLayout } = shaderAssembler.assembleWGSLShader({
                platformInfo,
                ...this.props,
                modules,
                defines,
                pluginInjections: resolvedPlugins.injections,
                pluginVertexInputs: resolvedPlugins.vertexInputs,
                pluginVaryings: resolvedPlugins.varyings
            });
            this.source = source;
            // @ts-expect-error
            this._getModuleUniforms = getUniforms;
            this._bindingTable = bindingTable;
            // Infer the layout after modules have been added so their bindings are included.
            const scannedOrReflectedShaderLayout = assembledShaderLayout ?? device.getShaderLayout?.(this.source);
            const inferredShaderLayout = normalizeShaderPluginAttributeNames(scannedOrReflectedShaderLayout, resolvedPlugins.vertexInputs);
            const shaderLayout = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeInferredShaderLayout"])(this.props.shaderLayout, inferredShaderLayout, Object.keys(resolvedPlugins.vertexInputs));
            this.props.shaderLayout = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["mergeShaderModuleBindingsIntoLayout"])(shaderLayout || null, modules) || null;
        } else {
            // GLSL
            const shaderAssembler = this.props.shaderAssembler;
            // GLSL shader pairs require an assembler with GLSL-specific hooks.
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$assert$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["assert"])(isShaderAssemblerForLanguage(shaderAssembler, 'glsl'));
            const { vs, fs, getUniforms } = shaderAssembler.assembleGLSLShaderPair({
                platformInfo,
                ...this.props,
                modules,
                defines,
                pluginInjections: resolvedPlugins.injections,
                pluginVertexInputs: resolvedPlugins.vertexInputs,
                pluginVaryings: resolvedPlugins.varyings
            });
            this.vs = vs;
            this.fs = fs;
            // @ts-expect-error
            this._getModuleUniforms = getUniforms;
            this._bindingTable = [];
        }
        this.vertexCount = this.props.vertexCount;
        this.indexCount = this.props.indexCount;
        this.firstVertex = this.props.firstVertex;
        this.firstIndex = this.props.firstIndex;
        this.instanceCount = this.props.instanceCount;
        this.topology = this.props.topology;
        this.bufferLayout = this.props.bufferLayout;
        this.parameters = this.props.parameters;
        // Seed attachment formats before creating the initial WebGPU pipeline. This is required for
        // depth-only models whose intentional empty color target list must not fall back to the canvas
        // format before the first render pass can synchronize attachment state.
        this._colorAttachmentFormats = this.props.colorAttachmentFormats;
        this._depthStencilAttachmentFormat = this.props.depthStencilAttachmentFormat;
        // Geometry, if provided, sets topology and vertex cound
        if (props.geometry) {
            this.setGeometry(props.geometry);
        }
        this.pipelineFactory = props.pipelineFactory || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$factories$2f$pipeline$2d$factory$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["PipelineFactory"].getDefaultPipelineFactory(this.device);
        this.shaderFactory = props.shaderFactory || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$factories$2f$shader$2d$factory$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ShaderFactory"].getDefaultShaderFactory(this.device);
        // Create the pipeline
        // @note order is important
        this.pipeline = this._updatePipeline();
        this.vertexArray = device.createVertexArray({
            shaderLayout: this.pipeline.shaderLayout,
            bufferLayout: this.pipeline.bufferLayout
        });
        // Now we can apply geometry attributes
        if (this._gpuGeometry) {
            this._setGeometryAttributes(this._gpuGeometry);
        }
        // Apply any dynamic settings that will not trigger pipeline change
        if ('isInstanced' in props) {
            this.isInstanced = props.isInstanced;
        }
        if (props.instanceCount) {
            this.setInstanceCount(props.instanceCount);
        }
        if (props.vertexCount) {
            this.setVertexCount(props.vertexCount);
        }
        if (props.indexBuffer) {
            this.setIndexBuffer(props.indexBuffer);
        }
        if (props.attributes) {
            this.setAttributes(props.attributes);
        }
        if (props.constantAttributes) {
            this.setConstantAttributes(props.constantAttributes);
        }
        if (props.bindings) {
            this.setBindings(props.bindings);
        }
        if (props.transformFeedback) {
            this.transformFeedback = props.transformFeedback;
        }
    }
    destroy() {
        if (!this._destroyed) {
            // Release pipeline before we destroy the shaders used by the pipeline
            this.pipelineFactory.release(this.pipeline);
            // Release the shaders
            this.shaderFactory.release(this.pipeline.vs);
            if (this.pipeline.fs && this.pipeline.fs !== this.pipeline.vs) {
                this.shaderFactory.release(this.pipeline.fs);
            }
            this._uniformStore.destroy();
            // TODO - mark resource as managed and destroyIfManaged() ?
            this._gpuGeometry?.destroy();
            this._destroyed = true;
        }
    }
    // Draw call
    /** Query redraw status. Clears the status. */ needsRedraw() {
        // Catch any writes to already bound resources
        if (this._getBindingsUpdateTimestamp() > this._lastDrawTimestamp) {
            this.setNeedsRedraw('contents of bound textures or buffers updated');
        }
        const needsRedraw = this._needsRedraw;
        this._needsRedraw = false;
        return needsRedraw;
    }
    /** Mark the model as needing a redraw */ setNeedsRedraw(reason) {
        this._needsRedraw ||= reason;
    }
    /** Returns WGSL binding debug rows for the assembled shader. Returns an empty array for GLSL models. */ getBindingDebugTable() {
        return this._bindingTable;
    }
    /**
     * Updates uniforms and pipeline state before opening a render pass.
     *
     * @param commandEncoder - Encoder that should own any GPU uploads emitted
     * during draw preparation.
     */ predraw(commandEncoder) {
        // Update uniform buffers if needed
        this._syncDynamicBuffers();
        this.updateShaderInputs(commandEncoder);
        this.material?.updateShaderInputs(commandEncoder);
        // Check if the pipeline is invalidated
        this.pipeline = this._updatePipeline();
    }
    /**
     * Issue one draw call.
     * @param renderPass - render pass to draw into
     * @returns `true` if the draw call was executed, `false` if resources were not ready.
     */ draw(renderPass) {
        if (this._drawBlockedReason && !this._pipelineNeedsUpdate) {
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].info(LOG_DRAW_PRIORITY, `>>> DRAWING ABORTED ${this.id}: ${this._drawBlockedReason}`)();
            return false;
        }
        const loadingBinding = this._areBindingsLoading();
        if (loadingBinding) {
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].info(LOG_DRAW_PRIORITY, `>>> DRAWING ABORTED ${this.id}: ${loadingBinding} not loaded`)();
            return false;
        }
        this._syncAttachmentFormats(renderPass);
        try {
            renderPass.pushDebugGroup(`${this}.predraw(${renderPass})`);
            if (this.device.type === 'webgpu') {
                // WebGPU uploads cannot be encoded once the render pass is already open.
                // Keep the implicit draw() path working for existing callers by falling
                // back to immediate writes here; callers that need upload ordering
                // across multiple draws/viewports must call predraw(commandEncoder)
                // before beginRenderPass().
                this.updateShaderInputs();
                this.material?.updateShaderInputs();
                this._syncDynamicBuffers();
                this.pipeline = this._updatePipeline();
            } else {
                this.predraw(this.device.commandEncoder);
            }
        } finally{
            renderPass.popDebugGroup();
        }
        let drawSuccess;
        let pipelineErrored = this.pipeline.isErrored;
        try {
            renderPass.pushDebugGroup(`${this}.draw(${renderPass})`);
            this._logDrawCallStart();
            // Update the pipeline if invalidated
            // TODO - inside RenderPass is likely the worst place to do this from performance perspective.
            // Application can call Model.predraw() to avoid this.
            this.pipeline = this._updatePipeline();
            pipelineErrored = this.pipeline.isErrored;
            if (pipelineErrored) {
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].info(LOG_DRAW_PRIORITY, `>>> DRAWING ABORTED ${this.id}: ${PIPELINE_INITIALIZATION_FAILED}`)();
                drawSuccess = false;
            } else {
                const drawValidationError = this.vertexArray.getDrawValidationError();
                if (drawValidationError) {
                    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].info(LOG_DRAW_PRIORITY, `>>> DRAWING ABORTED ${this.id}: ${drawValidationError}`)();
                    this._drawBlockedReason = drawValidationError;
                    drawSuccess = false;
                } else {
                    const shaderLayout = this._getCurrentShaderLayout();
                    const syncBindings = this._getBindings(shaderLayout);
                    const syncBindGroups = this._getBindGroups(shaderLayout, syncBindings);
                    const { indexBuffer } = this.vertexArray;
                    const indexCount = indexBuffer ? this.indexCount ?? indexBuffer.byteLength / (indexBuffer.indexType === 'uint32' ? 4 : 2) : undefined;
                    renderPass.setPipeline(this.pipeline);
                    renderPass.setBindings(syncBindGroups, {
                        _bindGroupCacheKeys: this._getBindGroupCacheKeys()
                    });
                    renderPass.setVertexArray(this.vertexArray);
                    const hasNoInstances = this.isInstanced === true && this.instanceCount === 0;
                    drawSuccess = hasNoInstances ? true : renderPass.draw({
                        isInstanced: this.isInstanced,
                        vertexCount: this.vertexCount,
                        instanceCount: this.isInstanced ? this.instanceCount : undefined,
                        indexCount,
                        firstVertex: this.firstVertex,
                        firstIndex: this.firstIndex,
                        transformFeedback: this.transformFeedback || undefined,
                        uniforms: this.props.uniforms,
                        // WebGL shares underlying cached programs even for models that have different
                        // parameters and topology, so those compatibility overrides remain per draw.
                        parameters: this.parameters,
                        topology: this.topology
                    });
                }
            }
        } finally{
            renderPass.popDebugGroup();
            this._logDrawCallEnd();
        }
        this._logFramebuffer(renderPass);
        // Update needsRedraw flag
        if (drawSuccess) {
            this._lastDrawTimestamp = this.device.timestamp;
            this._needsRedraw = false;
        } else if (pipelineErrored) {
            this._needsRedraw = PIPELINE_INITIALIZATION_FAILED;
            this._drawBlockedReason = PIPELINE_INITIALIZATION_FAILED;
        } else if (this._drawBlockedReason) {
            this._needsRedraw = this._drawBlockedReason;
        } else {
            this._needsRedraw = 'waiting for resource initialization';
        }
        return drawSuccess;
    }
    // Update fixed fields (can trigger pipeline rebuild)
    /**
     * Updates the optional geometry
     * Geometry, set topology and bufferLayout
     * @note Can trigger a pipeline rebuild / pipeline cache fetch on WebGPU
     */ setGeometry(geometry) {
        this._gpuGeometry?.destroy();
        const gpuGeometry = geometry && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$geometry$2f$gpu$2d$geometry$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["makeGPUGeometry"])(this.device, geometry);
        if (gpuGeometry) {
            this.setTopology(gpuGeometry.topology || 'triangle-list');
            const bufferLayoutHelper = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$buffer$2d$layout$2d$helper$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["BufferLayoutHelper"](this.bufferLayout);
            this.bufferLayout = bufferLayoutHelper.mergeBufferLayouts(gpuGeometry.bufferLayout, this.bufferLayout);
            if (this.vertexArray) {
                this._setGeometryAttributes(gpuGeometry);
            }
        }
        this._gpuGeometry = gpuGeometry;
    }
    /**
     * Updates the primitive topology ('triangle-list', 'triangle-strip' etc).
     * @note Triggers a pipeline rebuild / pipeline cache fetch on WebGPU
     */ setTopology(topology) {
        if (topology !== this.topology) {
            this.topology = topology;
            this._setPipelineNeedsUpdate('topology');
        }
    }
    /**
     * Updates the buffer layout.
     * @note Triggers a pipeline rebuild / pipeline cache fetch
     */ setBufferLayout(bufferLayout) {
        const bufferLayoutHelper = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$buffer$2d$layout$2d$helper$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["BufferLayoutHelper"](this.bufferLayout);
        const nextBufferLayout = this._gpuGeometry ? bufferLayoutHelper.mergeBufferLayouts(bufferLayout, this._gpuGeometry.bufferLayout) : bufferLayout;
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$deep$2d$equal$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["deepEqual"])(nextBufferLayout, this.bufferLayout, -1)) {
            return;
        }
        this.bufferLayout = nextBufferLayout;
        this._setPipelineNeedsUpdate('bufferLayout');
        // Recreate the pipeline
        this.pipeline = this._updatePipeline();
        // vertex array needs to be updated if we update buffer layout,
        // but not if we update parameters
        this.vertexArray = this.device.createVertexArray({
            shaderLayout: this.pipeline.shaderLayout,
            bufferLayout: this.pipeline.bufferLayout
        });
        // Reapply geometry attributes to the new vertex array
        if (this._gpuGeometry) {
            this._setGeometryAttributes(this._gpuGeometry);
        }
    }
    /**
     * Set GPU parameters.
     * @note Can trigger a pipeline rebuild / pipeline cache fetch.
     * @param parameters
     */ setParameters(parameters) {
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$deep$2d$equal$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["deepEqual"])(parameters, this.parameters, 2)) {
            this.parameters = parameters;
            this._setPipelineNeedsUpdate('parameters');
        }
    }
    // Update dynamic fields
    /**
     * Updates the instance count (used in draw calls)
     * @note Any attributes with stepMode=instance need to be at least this big
     */ setInstanceCount(instanceCount) {
        this.instanceCount = instanceCount;
        // luma.gl examples don't set props.isInstanced and rely on auto-detection
        // but deck.gl sets instanceCount even for models that are not instanced.
        if (this.isInstanced === undefined && instanceCount > 0) {
            this.isInstanced = true;
        }
        this.setNeedsRedraw('instanceCount');
    }
    /**
     * Updates the vertex count (used in draw calls)
     * @note Any attributes with stepMode=vertex need to be at least this big
     */ setVertexCount(vertexCount) {
        this.vertexCount = vertexCount;
        this.setNeedsRedraw('vertexCount');
    }
    /** Updates the indexed draw count override. */ setIndexCount(indexCount) {
        this.indexCount = indexCount;
        this.setNeedsRedraw('indexCount');
    }
    /** Updates the first indexed/non-indexed draw offsets. */ setDrawOffsets({ firstVertex, firstIndex }) {
        this.firstVertex = firstVertex;
        this.firstIndex = firstIndex;
        this.setNeedsRedraw('drawOffsets');
    }
    /** Set the shader inputs */ setShaderInputs(shaderInputs) {
        this.shaderInputs = shaderInputs;
        this._uniformStore = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$portable$2f$uniform$2d$store$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["UniformStore"](this.device, this.shaderInputs.modules);
        // Create uniform buffer bindings for all modules that actually have uniforms
        for (const [moduleName, module] of Object.entries(this.shaderInputs.modules)){
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$shader$2d$module$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["shaderModuleHasUniforms"])(module) && !this.material?.ownsModule(moduleName)) {
                const uniformBuffer = this._uniformStore.getManagedUniformBuffer(moduleName);
                this.bindings[`${moduleName}Uniforms`] = uniformBuffer;
            }
        }
        this.setNeedsRedraw('shaderInputs');
    }
    setMaterial(material) {
        this.material = material;
        this.setNeedsRedraw('material');
    }
    /** Update uniform buffers from the model's shader inputs */ /**
     * Flushes current shader-input values into managed uniform buffers and
     * non-material bindings.
     *
     * @param commandEncoder - Optional encoder used to order uniform uploads with
     * subsequent draw commands.
     */ updateShaderInputs(commandEncoder) {
        this._uniformStore.setUniforms(this.shaderInputs.getUniformValues(), commandEncoder);
        this.setBindings(this._getNonMaterialBindings(this.shaderInputs.getBindingValues()));
        // TODO - this is already tracked through buffer/texture update times?
        this.setNeedsRedraw('shaderInputs');
    }
    /**
     * Sets bindings (textures, samplers, uniform buffers)
     */ setBindings(bindings) {
        Object.assign(this.bindings, bindings);
        this.setNeedsRedraw('bindings');
    }
    /**
     * Updates optional transform feedback. WebGL only.
     */ setTransformFeedback(transformFeedback) {
        this.transformFeedback = transformFeedback;
        this.setNeedsRedraw('transformFeedback');
    }
    /**
     * Sets the index buffer
     * @todo - how to unset it if we change geometry?
     */ setIndexBuffer(indexBuffer) {
        const resolvedIndexBuffer = indexBuffer instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["DynamicBuffer"] ? indexBuffer.buffer : indexBuffer;
        this.indexBuffer = resolvedIndexBuffer;
        this._dynamicIndexBufferSource = indexBuffer instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["DynamicBuffer"] ? {
            source: indexBuffer,
            generation: indexBuffer.generation
        } : null;
        this.vertexArray.setIndexBuffer(resolvedIndexBuffer);
        this.setNeedsRedraw('indexBuffer');
    }
    /**
     * Sets attributes (buffers)
     * @note Overrides any attributes previously set with the same name
     */ setAttributes(buffers, options) {
        this._drawBlockedReason = false;
        const disableWarnings = options?.disableWarnings ?? this.props.disableWarnings;
        if (buffers['indices']) {
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].warn(`Model:${this.id} setAttributes() - indexBuffer should be set using setIndexBuffer()`)();
        }
        // ensure bufferLayout order matches source layout so we bind
        // the correct buffers to the correct indices in webgpu.
        this.bufferLayout = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$buffer$2d$layout$2d$order$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["sortedBufferLayoutByShaderSourceLocations"])(this.pipeline.shaderLayout, this.bufferLayout);
        const bufferLayoutHelper = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$buffer$2d$layout$2d$helper$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["BufferLayoutHelper"](this.bufferLayout);
        // Check if all buffers have a layout
        for (const [bufferName, buffer] of Object.entries(buffers)){
            const resolvedBuffer = buffer instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["DynamicBuffer"] ? buffer.buffer : buffer;
            const bufferLayout = bufferLayoutHelper.getBufferLayout(bufferName);
            if (!bufferLayout) {
                if (!disableWarnings) {
                    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].warn(`Model(${this.id}): Missing layout for buffer "${bufferName}".`)();
                }
                continue; // eslint-disable-line no-continue
            }
            // In WebGL, for an interleaved attribute we may need to set multiple attributes
            // but in WebGPU, we set it according to the buffer's position in the vertexArray
            const attributeNames = bufferLayoutHelper.getAttributeNamesForBuffer(bufferLayout);
            let set = false;
            for (const attributeName of attributeNames){
                const attributeInfo = this._attributeInfos[attributeName];
                if (attributeInfo) {
                    const bufferSlot = this.device.type === 'webgpu' ? this.vertexArray.getBufferSlot(attributeInfo.bufferName) : attributeInfo.location;
                    if (bufferSlot === null) {
                        if (!disableWarnings) {
                            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].warn(`Model(${this.id}): Missing vertex array slot for buffer "${attributeInfo.bufferName}".`)();
                        }
                        continue; // eslint-disable-line no-continue
                    }
                    this.vertexArray.setBuffer(bufferSlot, resolvedBuffer);
                    if (buffer instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["DynamicBuffer"]) {
                        this._dynamicAttributeBufferSources[bufferSlot] = {
                            source: buffer,
                            generation: buffer.generation
                        };
                    } else {
                        delete this._dynamicAttributeBufferSources[bufferSlot];
                    }
                    set = true;
                }
            }
            if (!set && !disableWarnings) {
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].warn(`Model(${this.id}): Ignoring buffer "${resolvedBuffer.id}" for unknown attribute "${bufferName}"`)();
            }
        }
        this.setNeedsRedraw('attributes');
    }
    /**
     * Sets constant attributes
     * @note Overrides any attributes previously set with the same name
     * Constant attributes are only supported in WebGL, not in WebGPU
     * Any attribute that is disabled in the current vertex array object
     * is read from the context's global constant value for that attribute location.
     * @param constantAttributes
     */ setConstantAttributes(attributes, options) {
        for (const [attributeName, value] of Object.entries(attributes)){
            const attributeInfo = this._attributeInfos[attributeName];
            if (attributeInfo) {
                this.vertexArray.setConstantWebGL(attributeInfo.location, value);
            } else if (!(options?.disableWarnings ?? this.props.disableWarnings)) {
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].warn(`Model "${this.id}: Ignoring constant supplied for unknown attribute "${attributeName}"`)();
            }
        }
        this.setNeedsRedraw('constants');
    }
    // INTERNAL METHODS
    /** Check that bindings are loaded. Returns id of first binding that is still loading. */ _areBindingsLoading() {
        for (const binding of Object.values(this.bindings)){
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$texture$2f$texture$2d$binding$2d$source$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["isTextureBindingSource"])(binding) && !binding.isReady) {
                return binding.id;
            }
        }
        for (const binding of Object.values(this.material?.bindings || {})){
            if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$texture$2f$texture$2d$binding$2d$source$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["isTextureBindingSource"])(binding) && !binding.isReady) {
                return binding.id;
            }
        }
        return false;
    }
    /**
     * Resolves ready model bindings for the current shader layout.
     * @param shaderLayout Reflected bindings used to select copied or external texture resolution.
     */ _getBindings(shaderLayout = this._getCurrentShaderLayout()) {
        const validBindings = {};
        for (const [name, binding] of Object.entries(this.bindings)){
            const resolvedBinding = resolveModelBinding(name, binding, shaderLayout);
            if (resolvedBinding) {
                validBindings[name] = resolvedBinding;
            }
        }
        return validBindings;
    }
    /**
     * Groups resolved model and material bindings for the current shader layout.
     * @param shaderLayout Reflected bindings used to group logical binding names.
     * @param bindings Model bindings already resolved for this draw preparation.
     */ _getBindGroups(shaderLayout = this._getCurrentShaderLayout(), bindings = this._getBindings(shaderLayout)) {
        const bindGroups = shaderLayout.bindings.length ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$bind$2d$groups$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["normalizeBindingsByGroup"])(shaderLayout, bindings) : {
            0: bindings
        };
        if (!this.material) {
            return bindGroups;
        }
        for (const [groupKey, groupBindings] of Object.entries(this.material.getBindingsByGroup(shaderLayout))){
            const group = Number(groupKey);
            bindGroups[group] = {
                ...bindGroups[group] || {},
                ...groupBindings
            };
        }
        return bindGroups;
    }
    _getBindGroupCacheKeys() {
        const bindGroupCacheKey = this.material?.getBindGroupCacheKey(3);
        return bindGroupCacheKey ? {
            3: bindGroupCacheKey
        } : {};
    }
    /** Get the timestamp of the latest updated bound GPU memory resource (buffer/texture). */ _getBindingsUpdateTimestamp() {
        let timestamp = 0;
        if (this._dynamicIndexBufferSource) {
            timestamp = Math.max(timestamp, this._dynamicIndexBufferSource.source.updateTimestamp);
        }
        for (const entry of Object.values(this._dynamicAttributeBufferSources)){
            timestamp = Math.max(timestamp, entry.source.updateTimestamp);
        }
        for (const binding of Object.values(this.bindings)){
            if (binding instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$texture$2d$view$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["TextureView"]) {
                timestamp = Math.max(timestamp, binding.texture.updateTimestamp);
            } else if (binding instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Buffer"] || binding instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$texture$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Texture"] || binding instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$external$2d$texture$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["ExternalTexture"]) {
                timestamp = Math.max(timestamp, binding.updateTimestamp);
            } else if (binding instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["DynamicBuffer"]) {
                timestamp = Math.max(timestamp, binding.updateTimestamp);
            } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$texture$2f$texture$2d$binding$2d$source$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["isTextureBindingSource"])(binding)) {
                timestamp = binding.isReady ? Math.max(timestamp, binding.updateTimestamp) : Infinity;
            } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["isBufferRangeBinding"])(binding)) {
                timestamp = Math.max(timestamp, binding.buffer instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["DynamicBuffer"] ? binding.buffer.updateTimestamp : binding.buffer.updateTimestamp);
            }
        }
        return Math.max(timestamp, this.material?.getBindingsUpdateTimestamp() || 0);
    }
    /**
     * Updates the optional geometry attributes
     * Geometry, sets several attributes, indexBuffer, and also vertex count
     * @note Can trigger a pipeline rebuild / pipeline cache fetch on WebGPU
     */ _setGeometryAttributes(gpuGeometry) {
        // Filter geometry attribute so that we don't issue warnings for unused attributes
        const attributes = {
            ...gpuGeometry.attributes
        };
        for (const [attributeName] of Object.entries(attributes)){
            if (!this.pipeline.shaderLayout.attributes.find((layout)=>layout.name === attributeName) && attributeName !== 'positions') {
                delete attributes[attributeName];
            }
        }
        // TODO - delete previous geometry?
        this.vertexCount = gpuGeometry.vertexCount;
        this.setIndexBuffer(gpuGeometry.indices || null);
        this.setAttributes(gpuGeometry.attributes, {
            disableWarnings: true
        });
        this.setAttributes(attributes, {
            disableWarnings: this.props.disableWarnings
        });
        this.setNeedsRedraw('geometry attributes');
    }
    /** Mark pipeline as needing update */ _setPipelineNeedsUpdate(reason) {
        this._pipelineNeedsUpdate ||= reason;
        this._drawBlockedReason = false;
        this.setNeedsRedraw(reason);
    }
    /** Update pipeline if needed */ _updatePipeline() {
        if (this._pipelineNeedsUpdate) {
            let prevShaderVs = null;
            let prevShaderFs = null;
            if (this.pipeline) {
                __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].log(1, `Model ${this.id}: Recreating pipeline because "${this._pipelineNeedsUpdate}".`)();
                prevShaderVs = this.pipeline.vs;
                prevShaderFs = this.pipeline.fs;
            }
            this._pipelineNeedsUpdate = false;
            const vs = this.shaderFactory.createShader({
                id: `${this.id}-vertex`,
                stage: 'vertex',
                source: this.source || this.vs,
                debugShaders: this.props.debugShaders
            });
            let fs = null;
            if (this.source) {
                fs = vs;
            } else if (this.fs) {
                fs = this.shaderFactory.createShader({
                    id: `${this.id}-fragment`,
                    stage: 'fragment',
                    source: this.source || this.fs,
                    debugShaders: this.props.debugShaders
                });
            }
            this.pipeline = this.pipelineFactory.createRenderPipeline({
                ...this.props,
                bindings: undefined,
                bufferLayout: this.bufferLayout,
                colorAttachmentFormats: this._colorAttachmentFormats,
                depthStencilAttachmentFormat: this._depthStencilAttachmentFormat,
                topology: this.topology,
                parameters: this.parameters,
                bindGroups: undefined,
                vs,
                fs
            });
            this._attributeInfos = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2d$utils$2f$get$2d$attribute$2d$from$2d$layouts$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getAttributeInfosFromLayouts"])(this.pipeline.shaderLayout, this.bufferLayout);
            if (prevShaderVs) this.shaderFactory.release(prevShaderVs);
            if (prevShaderFs && prevShaderFs !== prevShaderVs) {
                this.shaderFactory.release(prevShaderFs);
            }
        }
        return this.pipeline;
    }
    /** Throttle draw call logging */ _lastLogTime = 0;
    _logOpen = false;
    _logDrawCallStart() {
        // IF level is 4 or higher, log every frame.
        const logDrawTimeout = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].level > 3 ? 0 : LOG_DRAW_TIMEOUT;
        if (__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].level < 2 || Date.now() - this._lastLogTime < logDrawTimeout) {
            return;
        }
        this._lastLogTime = Date.now();
        this._logOpen = true;
        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].group(LOG_DRAW_PRIORITY, `>>> DRAWING MODEL ${this.id}`, {
            collapsed: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].level <= 2
        })();
    }
    _logDrawCallEnd() {
        if (this._logOpen) {
            const shaderLayoutTable = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$debug$2f$debug$2d$shader$2d$layout$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getDebugTableForShaderLayout"])(this.pipeline.shaderLayout, this.id);
            // log.table(logLevel, attributeTable)();
            // log.table(logLevel, uniformTable)();
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].table(LOG_DRAW_PRIORITY, shaderLayoutTable)();
            const uniformTable = this.shaderInputs.getDebugTable();
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].table(LOG_DRAW_PRIORITY, uniformTable)();
            const attributeTable = this._getAttributeDebugTable();
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].table(LOG_DRAW_PRIORITY, this._attributeInfos)();
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].table(LOG_DRAW_PRIORITY, attributeTable)();
            __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$utils$2f$log$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["log"].groupEnd(LOG_DRAW_PRIORITY)();
            this._logOpen = false;
        }
    }
    _drawCount = 0;
    _logFramebuffer(renderPass) {
        const debugFramebuffers = this.device.props.debugFramebuffers;
        this._drawCount++;
        // Update first 3 frames and then every 60 frames
        if (!debugFramebuffers) {
            // } || (this._drawCount++ > 3 && this._drawCount % 60)) {
            return;
        }
        const framebuffer = renderPass.props.framebuffer;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$debug$2f$debug$2d$framebuffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["debugFramebuffer"])(renderPass, framebuffer, {
            id: framebuffer?.id || `${this.id}-framebuffer`,
            minimap: true
        });
    }
    _getAttributeDebugTable() {
        const table = {};
        for (const [name, attributeInfo] of Object.entries(this._attributeInfos)){
            const values = this.vertexArray.attributes[attributeInfo.location];
            table[attributeInfo.location] = {
                name,
                type: attributeInfo.shaderType,
                values: values ? this._getBufferOrConstantValues(values, attributeInfo.bufferDataType) : 'null'
            };
        }
        if (this.vertexArray.indexBuffer) {
            const { indexBuffer } = this.vertexArray;
            const values = indexBuffer.indexType === 'uint32' ? new Uint32Array(indexBuffer.debugData) : new Uint16Array(indexBuffer.debugData);
            table['indices'] = {
                name: 'indices',
                type: indexBuffer.indexType,
                values: values.toString()
            };
        }
        return table;
    }
    // TODO - fix typing of luma data types
    _getBufferOrConstantValues(attribute, dataType) {
        const TypedArrayConstructor = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$shadertypes$2f$data$2d$types$2f$data$2d$type$2d$decoder$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["dataTypeDecoder"].getTypedArrayConstructor(dataType);
        const typedArray = attribute instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Buffer"] ? new TypedArrayConstructor(attribute.debugData) : attribute;
        return typedArray.toString();
    }
    _getNonMaterialBindings(bindings) {
        if (!this.material) {
            return bindings;
        }
        const filteredBindings = {};
        for (const [name, binding] of Object.entries(bindings)){
            if (!this.material.ownsBinding(name)) {
                filteredBindings[name] = binding;
            }
        }
        return filteredBindings;
    }
    /** Returns the current reflected shader layout or the pre-reflection empty layout. */ _getCurrentShaderLayout() {
        return this.pipeline?.shaderLayout || this.props.shaderLayout || {
            bindings: []
        };
    }
    _syncDynamicBuffers() {
        if (this._dynamicIndexBufferSource && this._dynamicIndexBufferSource.generation !== this._dynamicIndexBufferSource.source.generation) {
            const resolvedIndexBuffer = this._dynamicIndexBufferSource.source.buffer;
            this.indexBuffer = resolvedIndexBuffer;
            this.vertexArray.setIndexBuffer(resolvedIndexBuffer);
            this._dynamicIndexBufferSource.generation = this._dynamicIndexBufferSource.source.generation;
            this.setNeedsRedraw('dynamic index buffer');
        }
        for (const [locationKey, entry] of Object.entries(this._dynamicAttributeBufferSources)){
            if (entry.generation !== entry.source.generation) {
                this.vertexArray.setBuffer(Number(locationKey), entry.source.buffer);
                entry.generation = entry.source.generation;
                this.setNeedsRedraw('dynamic attribute buffer');
            }
        }
    }
    _syncAttachmentFormats(renderPass) {
        if (this.device.type !== 'webgpu') {
            return;
        }
        const framebuffer = renderPass.framebuffer || renderPass.props.framebuffer;
        const renderBundleProps = renderPass.props;
        const nextColorAttachmentFormats = renderBundleProps.colorAttachmentFormats ?? framebuffer?.colorAttachments?.map((colorAttachment)=>asColorAttachmentFormat(colorAttachment?.texture?.format));
        const nextDepthStencilAttachmentFormat = renderBundleProps.depthStencilAttachmentFormat === false ? undefined : renderBundleProps.depthStencilAttachmentFormat ?? asDepthStencilAttachmentFormat(framebuffer?.depthStencilAttachment?.texture?.format);
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$utils$2f$deep$2d$equal$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["deepEqual"])(this._colorAttachmentFormats, nextColorAttachmentFormats, 1) || this._depthStencilAttachmentFormat !== nextDepthStencilAttachmentFormat) {
            this._colorAttachmentFormats = nextColorAttachmentFormats;
            this._depthStencilAttachmentFormat = nextDepthStencilAttachmentFormat;
            this._setPipelineNeedsUpdate('attachment formats');
        }
    }
}
function isShaderAssemblerForLanguage(shaderAssembler, shaderLanguage) {
    if (shaderAssembler.shaderLanguage !== undefined && shaderAssembler.shaderLanguage !== shaderLanguage) {
        return false;
    }
    return shaderLanguage === 'glsl' ? 'assembleGLSLShaderPair' in shaderAssembler && typeof shaderAssembler.assembleGLSLShaderPair === 'function' : 'assembleWGSLShader' in shaderAssembler && typeof shaderAssembler.assembleWGSLShader === 'function';
}
function normalizeShaderPluginAttributeNames(shaderLayout, vertexInputs) {
    if (!shaderLayout || Object.keys(vertexInputs).length === 0) {
        return shaderLayout;
    }
    return {
        ...shaderLayout,
        attributes: shaderLayout.attributes.map((attribute)=>{
            const publicName = attribute.name.startsWith('_luma_') ? attribute.name.slice('_luma_'.length) : null;
            return publicName && vertexInputs[publicName] ? {
                ...attribute,
                name: publicName
            } : attribute;
        })
    };
}
/**
 * Resolves one model binding against the current shader layout.
 * @param bindingName Logical model binding name.
 * @param binding Model binding or deferred engine binding source.
 * @param shaderLayout Reflected bindings used to select copied or external texture resolution.
 * @returns Concrete core binding, or `null` while a deferred source is unavailable.
 */ function resolveModelBinding(bindingName, binding, shaderLayout) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$texture$2f$texture$2d$binding$2d$source$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["isTextureBindingSource"])(binding)) {
        const bindingLayout = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$texture$2f$texture$2d$binding$2d$source$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getTextureBindingLayout"])(shaderLayout, bindingName, {
            fallbackGroup: 0
        });
        return bindingLayout ? binding.resolveTextureBinding(bindingLayout) : null;
    }
    if (binding instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["DynamicBuffer"]) {
        return binding.buffer;
    }
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["isBufferRangeBinding"])(binding)) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$dynamic$2d$buffer$2f$dynamic$2d$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["resolveBufferRangeBinding"])(binding);
    }
    return binding;
}
function asColorAttachmentFormat(format) {
    return format && !isDepthStencilAttachmentFormat(format) ? format : null;
}
function asDepthStencilAttachmentFormat(format) {
    return format && isDepthStencilAttachmentFormat(format) ? format : undefined;
}
function isDepthStencilAttachmentFormat(format) {
    return DEPTH_STENCIL_ATTACHMENT_FORMATS.includes(format);
}
function getPlatformInfo(device) {
    return {
        type: device.type,
        shaderLanguage: device.info.shadingLanguage,
        shaderLanguageVersion: device.info.shadingLanguageVersion,
        gpu: device.info.gpu,
        limits: device.limits,
        // HACK - we pretend that the DeviceFeatures is a Set, it has a similar API
        features: device.features
    };
} //# sourceMappingURL=model.js.map
}),
"[project]/node_modules/@luma.gl/engine/dist/compute/buffer-transform.js [app-ssr] (ecmascript)", ((__turbopack_context__) => {
"use strict";

// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
__turbopack_context__.s([
    "BufferTransform",
    ()=>BufferTransform
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/core/dist/adapter/resources/buffer.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$glsl$2d$utils$2f$shader$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/shadertools/dist/lib/glsl-utils/shader-utils.js [app-ssr] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$model$2f$model$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@luma.gl/engine/dist/model/model.js [app-ssr] (ecmascript)");
;
;
;
const GL_INTERLEAVED_ATTRIBS = 0x8c8c;
const GL_SEPARATE_ATTRIBS = 0x8c8d;
class BufferTransform {
    device;
    model;
    transformFeedback;
    static defaultProps = {
        ...__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$model$2f$model$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Model"].defaultProps,
        feedbackBufferMode: 'separate',
        outputs: undefined,
        feedbackBuffers: undefined
    };
    static isSupported(device) {
        return device?.info?.type === 'webgl';
    }
    constructor(device, props = BufferTransform.defaultProps){
        if (!BufferTransform.isSupported(device)) {
            throw new Error('BufferTransform not yet implemented on WebGPU');
        }
        this.device = device;
        this.model = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$engine$2f$dist$2f$model$2f$model$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Model"](this.device, {
            id: props.id || 'buffer-transform-model',
            fs: props.fs || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$shadertools$2f$dist$2f$lib$2f$glsl$2d$utils$2f$shader$2d$utils$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["getPassthroughFS"])(),
            topology: props.topology || 'point-list',
            varyings: props.outputs || props.varyings,
            ...props,
            bufferMode: props.bufferMode || (props.feedbackBufferMode === 'interleaved' ? GL_INTERLEAVED_ATTRIBS : GL_SEPARATE_ATTRIBS)
        });
        this.transformFeedback = this.device.createTransformFeedback({
            layout: this.model.pipeline.shaderLayout,
            // @ts-expect-error TODO
            buffers: props.feedbackBuffers
        });
        this.model.setTransformFeedback(this.transformFeedback);
    }
    /** Destroy owned resources. */ destroy() {
        if (this.model) {
            this.model.destroy();
        }
    }
    /** @deprecated Use {@link destroy}. */ delete() {
        this.destroy();
    }
    /** Run one transform loop. */ run(options) {
        if (options?.inputBuffers) {
            this.model.setAttributes(options.inputBuffers);
        }
        if (options?.outputBuffers) {
            this.transformFeedback.setBuffers(options.outputBuffers);
        }
        const renderPass = this.device.beginRenderPass({
            discard: true,
            ...options
        });
        this.model.draw(renderPass);
        renderPass.end();
    }
    // DEPRECATED METHODS
    /** @deprecated App knows what buffers it is passing in - Returns the {@link Buffer} or {@link BufferRange} for given varying name. */ getBuffer(varyingName) {
        return this.transformFeedback.getBuffer(varyingName);
    }
    /** @deprecated App knows what buffers it is passing in - Reads the {@link Buffer} or {@link BufferRange} for given varying name. */ readAsync(varyingName) {
        const result = this.getBuffer(varyingName);
        if (!result) {
            throw new Error('BufferTransform#getBuffer');
        }
        if (result instanceof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$luma$2e$gl$2f$core$2f$dist$2f$adapter$2f$resources$2f$buffer$2e$js__$5b$app$2d$ssr$5d$__$28$ecmascript$29$__["Buffer"]) {
            return result.readAsync();
        }
        const { buffer, byteOffset = 0, byteLength = buffer.byteLength } = result;
        return buffer.readAsync(byteOffset, byteLength);
    }
} //# sourceMappingURL=buffer-transform.js.map
}),
];

//# sourceMappingURL=node_modules_%40luma_gl_engine_dist_b3d1f8a6._.js.map